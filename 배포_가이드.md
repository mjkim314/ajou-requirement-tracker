# 배포 가이드 — 아주 졸업요건 트래커

이 문서는 **코딩을 전혀 모르는 사람**도 따라 할 수 있도록 쓴 설명서입니다.
크게 두 가지를 다룹니다.

1. **내 컴퓨터에서 앱 켜고 끄기** (로컬 실행)
2. **인터넷에 올려서 누구나 접속하게 만들기** (무료 배포 — Cloudflare Pages 또는 GitHub Pages)

> 어려워 보이는 명령어가 나와도 걱정하지 마세요. 그대로 복사해서 붙여넣으면 됩니다.
> 화면에 뜨는 글자가 많아도 대부분은 "잘 되고 있다"는 뜻입니다.

---

## 이 앱에 대해 먼저 알아둘 것 (중요)

- 이 앱은 **서버가 필요 없는 웹페이지**입니다. 회원가입도, 데이터베이스도 없습니다.
- 입력한 과목·성적 같은 **내 데이터는 "그 브라우저 안"에만 저장**됩니다 (localStorage).
  - 그래서 **다른 사람이 이 사이트에 접속해도 내 데이터는 안 보입니다.** 각자 자기 것만 봅니다. (개인정보 걱정 없음)
  - 반대로, **다른 기기·다른 브라우저에서 열면 내 데이터가 안 보입니다.** 옮기려면 앱 안의 **백업(내보내기) 기능**으로 파일을 저장했다가 새 기기에서 복원하세요.
  - 사이트를 새 버전으로 다시 배포해도 **사용자가 이미 저장한 데이터는 지워지지 않습니다.**
- 화면 글꼴(Pretendard)과 아이콘은 인터넷에서 받아옵니다. 그래서 **인터넷이 연결돼 있어야** 디자인이 제대로 보입니다.

---

## 목차

- [1. 준비물: Node.js 설치](#1-준비물-nodejs-설치)
- [2. 내 컴퓨터에서 앱 켜고 끄기 (로컬 실행)](#2-내-컴퓨터에서-앱-켜고-끄기-로컬-실행)
- [3. 배포 전 딱 한 줄만 바꾸기 (`base` 설정)](#3-배포-전-딱-한-줄만-바꾸기-base-설정)
- [4. 코드를 GitHub에 올리기 (마우스 클릭만으로)](#4-코드를-github에-올리기-마우스-클릭만으로)
- [5. 방법 A — Cloudflare Pages (가장 쉬움, 추천)](#5-방법-a--cloudflare-pages-가장-쉬움-추천)
- [6. 방법 B — GitHub Pages](#6-방법-b--github-pages)
- [7. 배포한 뒤 내용을 수정하고 다시 올리기](#7-배포한-뒤-내용을-수정하고-다시-올리기)
- [8. 문제가 생겼을 때 (자주 겪는 증상)](#8-문제가-생겼을-때-자주-겪는-증상)
- [9. 어떤 방법을 고를까 — 한눈에 비교](#9-어떤-방법을-고를까--한눈에-비교)

---

## 1. 준비물: Node.js 설치

로컬에서 앱을 켜려면 **Node.js**라는 프로그램이 필요합니다. (한 번만 설치하면 됩니다.)

> Cloudflare Pages의 "직접 업로드" 방식으로만 배포할 계획이라도, 배포용 파일을 만들려면 Node.js가 필요합니다.

### 설치

1. 웹브라우저에서 **https://nodejs.org** 접속
2. **"LTS"** 라고 적힌 버튼을 눌러 내려받기 (LTS = 안정 버전)
3. 내려받은 파일을 실행하고, 나오는 대로 **"다음 / 계속 / 동의"** 를 눌러 설치

### 설치가 잘 됐는지 확인

**터미널**(명령어를 입력하는 검은 창)을 엽니다.

- **Mac**: `Command(⌘) + Space` → `터미널` 입력 → Enter
- **Windows**: 시작 메뉴에서 `PowerShell` 입력 → Enter

창이 열리면 아래를 입력하고 Enter:

```bash
node -v
```

`v20.x.x` 처럼 숫자가 나오면 성공입니다. (숫자는 달라도 됩니다.)
`command not found` 가 나오면 설치가 안 된 것이니, Node.js를 다시 설치하고 **터미널을 껐다 켜세요.**

---

## 2. 내 컴퓨터에서 앱 켜고 끄기 (로컬 실행)

여기서는 인터넷에 올리지 않고, **내 컴퓨터에서만** 앱을 열어봅니다.

### 2-1. 프로젝트 폴더로 이동하기

터미널에 `cd `(cd 뒤에 **한 칸 띄우기**)를 입력한 다음, **프로젝트 폴더를 터미널 창으로 드래그**하고 Enter를 누르세요. 경로가 자동으로 채워집니다.

직접 입력한다면 이렇게 됩니다:

```bash
cd /Users/min/Desktop/ajou_requirement_tracker
```

> **팁**: 지금 어느 폴더에 있는지 헷갈리면 `pwd`(Mac) 를 입력해 확인하세요. 폴더 안 파일 목록은 `ls`(Mac) 로 볼 수 있습니다. `package.json` 이라는 파일이 보이면 제대로 온 것입니다.

### 2-2. 최초 1회만: 필요한 부품 내려받기

```bash
npm install
```

- 이 명령은 앱이 돌아가는 데 필요한 부품들(라이브러리)을 인터넷에서 받아 `node_modules` 폴더에 넣습니다.
- **처음 한 번만** 하면 됩니다. (몇 분 걸릴 수 있고, 노란 경고 메시지가 나와도 대부분 무시해도 됩니다.)

### 2-3. 개발 서버 켜기

```bash
npm run dev
```

이렇게 생긴 안내가 나옵니다:

```
  VITE v5.4.21  ready in 400 ms

  ➜  Local:   http://localhost:5173/
  ➜  press h + enter to show help
```

브라우저 주소창에 **http://localhost:5173** 을 입력하면 앱이 열립니다.
(`localhost` 는 "내 컴퓨터"라는 뜻입니다. 이 주소는 나만 볼 수 있습니다.)

> **개발 서버의 장점**: 켜 둔 상태에서 코드를 고치면 브라우저가 **자동으로 새로고침**되어 바로 반영됩니다.

### 2-4. 서버 끄기

터미널 창을 클릭한 상태에서 **`Control + C`** 를 누르세요. (Mac도 `Command`가 아니라 `Control` 입니다.)
서버가 멈추고 다시 명령을 입력할 수 있게 됩니다. 터미널 창을 그냥 닫아도 꺼집니다.

### 2-5. (선택) 실제 배포본 미리보기

인터넷에 올라갈 "완성된 형태"를 미리 확인하고 싶다면:

```bash
npm run build      # 배포용 파일을 dist 폴더에 만듭니다
npm run preview    # 그 결과물을 실제처럼 띄워봅니다
```

`npm run preview` 는 보통 **http://localhost:4173** 주소를 알려줍니다. 마찬가지로 `Control + C` 로 끕니다.

| 명령어 | 하는 일 | 언제 쓰나 |
|---|---|---|
| `npm install` | 필요한 부품 설치 | 최초 1회, 또는 부품이 바뀌었을 때 |
| `npm run dev` | 개발용 서버 켜기 (자동 새로고침) | 만들면서 확인할 때 |
| `npm run build` | 배포용 파일 생성 (`dist` 폴더) | 인터넷에 올리기 직전 |
| `npm run preview` | 배포본을 실제처럼 미리보기 | 올리기 전 최종 점검 |

---

## 3. 배포 전 딱 한 줄만 바꾸기 (`base` 설정)

인터넷에 올리기 전에 **설정 파일 한 곳에 한 줄**만 추가하면 됩니다. 이 한 줄이 **가장 흔한 배포 실패(흰 화면)를 막아 줍니다.**

### 왜 필요한가요?

GitHub Pages에 올리면 주소가 보통 `내이름.github.io/저장소이름/` 처럼 **뒤에 폴더 이름이 붙은 주소**가 됩니다. 그런데 앱은 기본적으로 파일을 "맨 앞 주소"에서 찾으려 해서, 파일을 못 찾고 **화면이 하얗게** 뜹니다.

해결책은 파일을 **"현재 위치 기준"으로 찾게** 만드는 것입니다. 그게 `base: './'` 한 줄입니다.
이 앱은 페이지 이동(URL 라우팅)이 없는 단일 화면이라, **이 한 줄이면 Cloudflare(루트 주소)와 GitHub Pages(폴더 주소) 양쪽에서 모두 잘 작동합니다.** 저장소 이름을 바꿔도 안전합니다.

### 어떻게 바꾸나요?

프로젝트 폴더 안의 **`vite.config.ts`** 파일을 텍스트 편집기(메모장, VS Code 등)로 엽니다. 지금 내용은 이렇습니다:

```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
})
```

여기에 `base: './',` 한 줄을 추가해서 이렇게 만듭니다:

```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  base: './',
  plugins: [react(), tailwindcss()],
})
```

저장하면 끝입니다. (`plugins` 앞에 `base: './',` 줄을 넣고, 줄 끝의 **쉼표**를 빠뜨리지 마세요.)

> 이 변경이 부담스러우면, 저에게 "vite.config.ts에 base 넣어줘" 라고 말해 주세요. 대신 넣어 드리겠습니다.

---

## 4. 코드를 GitHub에 올리기 (마우스 클릭만으로)

Cloudflare Pages의 **Git 연결 방식**과 GitHub Pages는 모두, 먼저 코드가 **GitHub**라는 사이트에 올라가 있어야 합니다.
명령어 없이 **GitHub Desktop**이라는 무료 프로그램으로 클릭만 해서 올릴 수 있습니다.

> **GitHub 없이 배포하고 싶다면?** [5-B. 직접 업로드 방식](#5-b-직접-업로드-방식-github-불필요)으로 건너뛰세요. `dist` 폴더를 드래그해서 올리는 방법이라 GitHub이 필요 없습니다.

### 4-1. GitHub 계정 만들기

1. **https://github.com** 접속 → 오른쪽 위 **Sign up**
2. 이메일·비밀번호·사용자이름(username) 입력
3. GitHub이 보낸 **인증 메일**을 열어 확인 (인증해야 저장소를 만들 수 있습니다)
4. 요금제는 **Free(무료)** 로 충분합니다

> **사용자이름(username)** 은 나중에 사이트 주소의 일부가 됩니다. 예: 사용자이름이 `mjkim314` 면 GitHub Pages 주소는 `mjkim314.github.io/...` 가 됩니다. 소문자로 깔끔하게 정하세요.

### 4-2. GitHub Desktop 설치 & 로그인

1. **https://desktop.github.com** 접속 → 큰 다운로드 버튼 클릭 (Mac/Windows 자동 인식)
2. 설치 후 실행 → **Sign in to GitHub.com** → 브라우저에서 **Authorize** → 앱으로 돌아오기
3. 이름/이메일을 물으면 기본값으로 **계속(Continue)**

### 4-3. 프로젝트 폴더 올리기 (첫 업로드)

1. GitHub Desktop 상단 메뉴 **File → Add Local Repository…**
   (또는 프로젝트 폴더를 앱 창으로 그냥 **드래그**)
2. 프로젝트 폴더(`ajou_requirement_tracker`)를 선택 → **Add Repository**
   - 이 폴더는 이미 Git 저장소라서 바로 열립니다.
3. 왼쪽 **Changes** 목록에 파일들이 뜹니다. 왼쪽 아래 칸에 요약(예: `first commit`)을 적고 파란 버튼 **Commit to main** 클릭
   - *Commit(커밋) = 내 컴퓨터에 스냅샷 저장.*
4. 상단에 **Publish repository** 버튼이 나타납니다. 클릭 →
   - **Name**: 저장소 이름 (그대로 두어도 됨)
   - **Keep this code private** 체크박스: 아래 설명 참고
   - **Publish repository** 클릭 → GitHub에 업로드 완료!

> **공개(Public) vs 비공개(Private) — 무료 기준 중요**
> - **GitHub Pages**로 배포하려면 저장소를 **공개(Public)** 로 해야 합니다. (비공개 저장소의 Pages는 유료)
>   → GitHub Pages를 쓸 거면 **`Keep this code private` 체크를 해제**하세요.
> - **Cloudflare Pages**는 **비공개 저장소도 무료로 배포**됩니다. 코드를 비공개로 두고 싶다면 Cloudflare를 쓰세요.
> - 참고: 저장소를 비공개로 둬도 **배포된 웹사이트 자체는 누구나 접속 가능**합니다. 비공개가 되는 건 "소스 코드"이지 "사이트"가 아닙니다.

> **`dist` 와 `node_modules` 는 안 올라가는 게 정상입니다.** 이 프로젝트의 `.gitignore` 파일이 그 둘을 일부러 제외합니다. 배포 서비스가 클라우드에서 알아서 다시 만들기 때문에 올릴 필요가 없습니다.

---

## 5. 방법 A — Cloudflare Pages (가장 쉬움, 추천)

**추천 이유**: 설정 파일(YAML)을 만들 필요가 없고, **비공개 저장소도 무료**이며, Vite를 자동으로 알아봅니다.

두 가지 방식이 있습니다:
- **5-A. Git 연결 방식** — GitHub과 연결해두면, 앞으로 수정 후 올릴(push) 때마다 **자동으로 다시 배포**됩니다. (추천)
- **5-B. 직접 업로드 방식** — `dist` 폴더를 드래그해서 올립니다. **GitHub이 필요 없습니다.**

> 시작 전에 [3장](#3-배포-전-딱-한-줄만-바꾸기-base-설정)의 `base: './'` 를 넣어두면 안전합니다. (Cloudflare는 루트 주소라 없어도 대개 되지만, 넣어두면 두 방법 다 문제없습니다.)

### 5-A. Git 연결 방식 (자동 배포)

먼저 [4장](#4-코드를-github에-올리기-마우스-클릭만으로)대로 코드를 GitHub에 올려두세요.

1. **https://dash.cloudflare.com** 접속 → (무료) 계정 로그인/가입
2. 왼쪽 메뉴 **Workers & Pages** → **Create application** 버튼
3. 상단 **Pages** 탭 선택 → **Import an existing Git repository**(예전 이름: *Connect to Git*)
4. GitHub 연결을 처음 하면 **Authorize(권한 허용)** → 목록에서 내 저장소 선택
5. **Set up builds and deployments**(빌드 설정) 화면에서:
   | 항목 | 넣을 값 |
   |---|---|
   | Production branch | `main` |
   | Framework preset | **Vite** (또는 `React (Vite)`) |
   | Build command | `npm run build` |
   | Build output directory | `dist` |
   - **Framework preset**을 고르면 아래 두 칸이 자동으로 채워집니다. `dist` 가 맞는지만 확인하세요.
6. **Save and Deploy** 클릭
7. 잠시 기다리면 빌드가 끝나고 **`프로젝트이름.pages.dev`** 주소가 나옵니다. 그 주소가 여러분의 사이트입니다. 🎉

> 이후에는 GitHub Desktop에서 수정사항을 **Push** 하기만 하면 Cloudflare가 **자동으로 다시 배포**합니다. ([7장](#7-배포한-뒤-내용을-수정하고-다시-올리기) 참고)

### 5-B. 직접 업로드 방식 (GitHub 불필요)

GitHub 없이 가장 빠르게 올리는 방법입니다.

1. 먼저 내 컴퓨터에서 배포용 파일을 만듭니다 (터미널, 프로젝트 폴더에서):
   ```bash
   npm run build
   ```
   → `dist` 폴더가 생깁니다. (안에 `index.html` 과 `assets` 폴더가 들어 있습니다.)
2. **https://dash.cloudflare.com** → **Workers & Pages** → **Create application** → **Pages** 탭
3. **Upload assets**(또는 **Get started**) → **Drag and drop your files** 선택
4. 프로젝트 이름을 정하고, **`dist` 폴더 전체를 드롭 영역으로 드래그**
5. **Deploy site** 클릭 → 완료. `프로젝트이름.pages.dev` 주소가 나옵니다.

> 나중에 새 버전을 올리려면: 다시 `npm run build` 로 `dist` 를 새로 만든 뒤, 그 프로젝트에서 **Create a new deployment** 을 눌러 `dist` 를 또 드래그하면 됩니다.

> ⚠️ **주의**: 직접 업로드로 만든 프로젝트는 **나중에 Git 자동배포로 바꿀 수 없습니다.** 자동 배포를 원하면 5-A 방식으로 새로 만들어야 합니다.

### (선택) Cloudflare 추가 설정

- **무료 주소**: 모든 프로젝트는 만들 때 자동으로 `프로젝트이름.pages.dev` 주소를 받습니다. 별도 설정 불필요.
- **내 도메인 연결**: 프로젝트 → **Custom domains** 탭 → **Set up a domain** → 도메인 입력 → 안내대로 DNS 설정.
- **Node 버전 지정(대개 불필요)**: Git 방식에서 빌드가 Node 버전 때문에 실패하면, 프로젝트 **Settings → Environment variables** 에서 `NODE_VERSION` 을 `20` 등으로 추가하고 다시 배포하세요.

---

## 6. 방법 B — GitHub Pages

GitHub만으로 배포합니다. **저장소가 공개(Public)** 여야 무료입니다.

준비:
- [4장](#4-코드를-github에-올리기-마우스-클릭만으로)대로 코드를 GitHub에 올려두기 (**공개**로)
- [3장](#3-배포-전-딱-한-줄만-바꾸기-base-설정)의 **`base: './'` 를 반드시 넣기** (안 넣으면 흰 화면이 됩니다)

### 6-1. 배포 설정 파일 만들기

GitHub Pages는 클라우드에서 앱을 대신 빌드해 줍니다. 그러려면 **설정 파일 하나**를 프로젝트에 넣어야 합니다.

1. 프로젝트 폴더 안에 **`.github`** 폴더를 만들고, 그 안에 **`workflows`** 폴더를 만듭니다.
   (폴더 이름 맨 앞의 **점(.)** 을 빠뜨리지 마세요.)
2. 그 안에 **`deploy.yml`** 파일을 만들고, 아래 내용을 **그대로** 붙여넣습니다:

```yaml
# 아주 졸업요건 트래커 — GitHub Pages 자동 배포
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]      # main 브랜치에 올릴 때마다 실행
  workflow_dispatch:       # 수동 실행 버튼도 켜기

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: lts/*
          cache: 'npm'
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run build
      - name: Setup Pages
        uses: actions/configure-pages@v6
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: './dist'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
```

> 📌 **버전 안내**: 위 파일의 `@v4`, `@v5`, `@v3` 같은 번호는 도구 버전입니다. 시간이 지나 배포가 실패하면, 항상 최신 정답이 실려 있는 **공식 Vite 가이드**에서 그대로 복사해 오세요 → **https://vite.dev/guide/static-deploy** 의 "GitHub Pages" 부분.
> (`upload-pages-artifact` 와 `deploy-pages` 는 **세대를 맞춰야** 합니다. 공식 가이드 것을 통째로 복사하면 자동으로 맞습니다.)

3. 이 파일을 GitHub Desktop에서 **Commit → Push** 해서 GitHub에 올립니다. ([7장](#7-배포한-뒤-내용을-수정하고-다시-올리기) 참고)

### 6-2. GitHub에서 Pages 켜기 (최초 1회)

1. 브라우저에서 내 저장소(github.com/...) 로 이동
2. 상단 **Settings** 탭 → 왼쪽 사이드바 **Code and automation** 아래 **Pages**
3. **Build and deployment** 항목의 **Source** 드롭다운을 **GitHub Actions** 로 변경
   - 이 방식은 브랜치나 폴더를 따로 고르지 않습니다. 선택 즉시 적용됩니다.
   - ⚠️ 이 설정을 **GitHub Actions로 바꾸지 않으면** 배포가 실패합니다.

### 6-3. 배포 확인

1. 저장소 상단 **Actions** 탭으로 이동하면 방금 올린 배포 작업이 돌고 있습니다.
2. 초록색 체크(✓)가 뜨면 성공. **Deploy to GitHub Pages** 단계에 사이트 주소가 표시됩니다.
3. 주소는 보통 **`https://내사용자이름.github.io/저장소이름/`** 입니다.
   - 예: `https://mjkim314.github.io/ajou_requirement_tracker/`

> **첫 배포는 몇 분 걸릴 수 있고**, 잠깐 404가 뜨다가 곧 열립니다. 조금 기다려 주세요.
> **흰 화면**이 뜨면 [3장의 `base` 설정](#3-배포-전-딱-한-줄만-바꾸기-base-설정)을 안 넣었을 가능성이 큽니다. 넣고 다시 Push 하세요.

---

## 7. 배포한 뒤 내용을 수정하고 다시 올리기

앱을 고친 뒤 반영하는 과정은 아주 간단합니다.

### Cloudflare(Git 방식) 또는 GitHub Pages 를 쓰는 경우

1. 코드를 수정하고 저장합니다. (필요하면 `npm run dev` 로 확인)
2. **GitHub Desktop** 을 엽니다 → 바뀐 파일이 **Changes** 에 보입니다.
3. 아래 요약칸에 간단히 적고 (예: `요약 탭 문구 수정`) → **Commit to main**
4. 상단 **Push origin** 클릭 (숫자는 "올릴 커밋 개수"입니다)
5. 끝. 몇 분 안에 사이트가 **자동으로 새 버전으로 바뀝니다.**
   - *Commit = 내 컴퓨터에 저장 / Push = GitHub에 올리기. 둘 다 해야 반영됩니다.*

### Cloudflare(직접 업로드 방식) 를 쓰는 경우

1. `npm run build` 로 `dist` 를 새로 만듭니다.
2. Cloudflare 프로젝트에서 **Create a new deployment** → 새 `dist` 폴더를 드래그.

> **사용자 데이터는 그대로 유지됩니다.** 새 버전을 올려도 사람들이 이미 입력한 데이터는 각자 브라우저에 남아 있으니 안심하세요.

---

## 8. 문제가 생겼을 때 (자주 겪는 증상)

| 증상 | 원인 | 해결 |
|---|---|---|
| **사이트가 하얗게만 뜬다** (특히 GitHub Pages) | `base` 설정 누락 | [3장](#3-배포-전-딱-한-줄만-바꾸기-base-설정)의 `base: './'` 를 넣고 다시 Push |
| **글꼴/아이콘이 깨져 보인다** | 인터넷 미연결 (폰트·아이콘을 인터넷에서 받음) | 인터넷 연결 확인. 오프라인에서도 기능은 동작하지만 디자인만 기본 글꼴로 보임 |
| **`command not found: npm`** | Node.js 미설치 | [1장](#1-준비물-nodejs-설치)대로 설치 후 터미널 재실행 |
| **`npm run dev` 가 안 되고 에러** | 부품 미설치 | 먼저 `npm install` 실행 |
| **`localhost:5173` 접속 안 됨** | 서버가 안 켜져 있음 | 터미널에서 `npm run dev` 가 실행 중인지 확인 |
| **포트가 이미 사용 중(port in use)** | 이전 서버가 안 꺼짐 | 그 터미널에서 `Control + C`. Vite가 자동으로 다른 포트(5174 등)를 잡아주기도 함 |
| **빌드 실패(`npm run build` 에러)** | 코드에 오타/타입 오류 | 에러 메시지의 파일명·줄번호 확인. 그대로 저에게 붙여넣어 물어보세요 |
| **GitHub Pages가 404/배포 실패** | Source가 GitHub Actions로 안 돼 있음 | [6-2](#6-2-github에서-pages-켜기-최초-1회) 확인. Settings → Pages → Source = GitHub Actions |
| **Cloudflare 빌드 실패** | 빌드 설정 오류 | Build output이 `dist` 인지, Build command가 `npm run build` 인지 확인 |
| **다른 기기에서 내 데이터가 안 보인다** | localStorage는 기기·브라우저마다 별개 | 앱의 **백업(내보내기)** 로 파일 저장 → 새 기기에서 **복원(불러오기)** |

> 그래도 막히면, **에러 메시지 전체를 그대로 복사해서** 저에게 붙여넣어 주세요. 어디가 문제인지 짚어 드리겠습니다.

---

## 9. 어떤 방법을 고를까 — 한눈에 비교

| | **Cloudflare — Git 연결** | **Cloudflare — 직접 업로드** | **GitHub Pages** |
|---|---|---|---|
| 난이도 | ★★☆ 쉬움 | ★☆☆ 가장 쉬움 | ★★★ 보통 (설정 파일 필요) |
| GitHub 필요? | 필요 | **불필요** | 필요 |
| 설정 파일(YAML) | 없음 | 없음 | 있음 (`deploy.yml`) |
| 비공개 코드 가능? | ✅ 무료 | 해당 없음 | ❌ (공개만 무료) |
| 수정 후 반영 | Push하면 **자동** | `dist` 다시 드래그 | Push하면 **자동** |
| `base` 설정 | 없어도 대개 OK | 없어도 대개 OK | **반드시 필요** |
| 주소 | `이름.pages.dev` | `이름.pages.dev` | `이름.github.io/저장소/` |

### 추천

- **가장 쉽고 편하게, 계속 관리할 거면** → **Cloudflare Pages · Git 연결** (5-A)
- **딱 한 번, GitHub도 안 만들고 빠르게** → **Cloudflare Pages · 직접 업로드** (5-B)
- **이미 GitHub을 쓰고 있고 코드를 공개해도 되면** → **GitHub Pages** (6장)

---

*이 앱의 졸업 판정은 참고용입니다. 최종 확인은 반드시 학과·학사팀을 통해 하세요.*
