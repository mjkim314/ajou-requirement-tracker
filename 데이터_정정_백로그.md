# 데이터·코드 정정 백로그

요람 **신규 추출**(`데이터화_진행률.md`)과 별개로, 검토 과정에서 확인됐지만 아직 반영하지 않은 **기존 데이터·코드 정정 항목**입니다. 한 항목씩 "N번 해줘"라고 지시하면 처리하고 상태(⬜→✅)를 갱신합니다.

---

## 1. ✅ 어학인증 대안 2종 누락 — NEW TEPS·IELTS *(2026-07-23 처리)*

2021·2022 SW 요건 세트의 `english_cert`가 9종만 담고 있었음. 어학졸업인증제 운영규칙(3-3-7, 2025.11.24 개정) [별표1] 소프트웨어학과 행 기준 **NEW TEPS 329, IELTS 5.5** 추가.

- 처리: 4개 세트(2021·2022 × advanced/general) `alternatives` 9→11종, `sourceNote`에 [별표1] 근거 병기
- 검증: `src/engine/__tests__/data-corrections.test.ts`가 4개 세트 전부의 11종·수치 고정

## 2. ✅ `bundleFor()` 폴백이 SW 전용 — (학과, 학번) 기반으로 정리 *(2026-07-23 처리)*

`src/app/dataSources.ts`의 `bundleFor()`가 미매핑 세트에 학번 추론만으로 SW 카탈로그를 물려주던 것을 정리.

- 처리: 폴백을 (학과, 학번) 기반으로 변경 — SW 세트(또는 학과 미기재 레거시)만 학번으로 카탈로그 연도를 고르고, **비SW 학과 세트는 빈 번들(카탈로그 미지정)** 반환. 기존 사용자(SW·레거시 커스텀 세트) 동작은 불변
- `additionalMajorTemplates()`: `department` 인자 추가 — 비SW 학과에는 전학교 공통 유형(복수·부·트랙·마이크로 등)만 보여주고 SW 참여 연계전공은 제외. 온보딩 호출부(`AdditionalMajorStep`)에 `draft.department` 전달
- 검증: `src/app/dataSources.test.ts` 9건
- 남은 개선(선택): 빈 카탈로그 상태의 UI 안내 문구(과목 자동완성 빈 결과에 "이 요건 세트에는 카탈로그가 없어요" 등) — 비SW 프리셋을 실제 출시할 때 함께

## 3. ✅ 학점인정 상한 미모델링 (편입·교환·계절 교류) *(2026-07-23 처리)*

학칙 제45조 등 학점인정 상한이 앱에 없던 것을 **경고 전용**으로 모델링.

- 처리: 엔진에 `RequirementSet.recognitionCaps` 추가(`states` 합산 → `maxCredits`/`maxRatioOfTotal` 중 작은 쪽 상한). 초과 시 `warning` 심각도로만 알리고 **verdict·학점 집계는 불변**(실제 인정 여부는 학교 판단). 숫자는 전부 JSON에서(하드코딩 없음)
- 데이터: 4개 세트에 대표 상한 1건 탑재 — `transferred`+`credited` 합산 ≤ 졸업학점의 1/2(학칙 제45조2항). 유형별 세부 상한(어학연수 12, 입학 전 6 등)은 과목 상태로 구분 불가해 미구현 — `sourceNote`에 명시
- 함께: 공유 세트 화이트리스트(`storage/backup.ts`)에 `recognitionCaps` 추가, 스킬 문서(`schema.md`·`regulations.md` §7) 갱신
- 검증: `src/engine/__tests__/recognition-caps.test.ts` 6건(초과 경고·경계·상태 필터·min 적용·하위 호환·판정 불변)

## 4. ✅ 일반과정 산학프로젝트 인증 미검사 *(2026-07-24 처리 — R3b, 요람 원문 확인으로 범위 축소)*

백로그 원 기록은 "일반 1개 이상·심화 2개 이상"이 **2021~2024 SW 요람 공통**이라고 적었으나, R3b 착수 시 요람 원문(pdftotext)을 4개년 전부 대조한 결과 **연도별로 다르다**:

| 요람 | 「기타 졸업요건」 문구 | 판정 |
|---|---|---|
| 2021·2022 | **"(심화과정 이수 시 필수)"** — 산학 2개 이상 | 심화 전용이 맞음. **현행 데이터 정확, 무변경** |
| 2023·2024 | 과정 한정 없음 — 산학 **"일반과정 이수자는 1개 이상**, 심화과정 이수자는 2개 이상" | 일반과정 미검사는 오류. **정정 대상** |
| 2025·2026 | **"심화과정 추가 졸업요건"** | 심화 전용이 맞음. 무변경 |

- 처리: `requirement-set-2023/2024-sw.json` **general 세트만** `industry_project`를 `appliesWhen null` + `pick 1`로 변경, `sourceNote`에 요람 근거 병기. advanced(pick 2)·2021/2022/2025/2026 무변경. 스냅샷 해시 2건 갱신.
- 검증: `data-2023-sw`·`data-2024-sw` B-05 재작성(일반 산학 활성·1개 충족) + B-05b 신설(산학 0개 → 졸업 불가), genCommon에 자기주도프로젝트 1과목 편입. 2021/2022 B-05/B-07엔 "심화 전용이 요람 원문과 일치" 주석 고정.
- **남은 판단(사용자 확인 대기)**: 2023·2024 요람은 「기타 졸업요건」의 심화 한정 문구 자체가 사라져 **major_ability(전공 역량 인증)·programming_cert(프로그래밍 역량 인증)도 일반과정에 적용된다고 읽을 수 있다**. 다만 산학처럼 "일반과정 …" 명시가 없고 2025 요람이 다시 심화 전용으로 되돌아가, 결정 ③의 기본값(심화 전용 유지)을 따랐다. 일반 적용으로 해석을 바꾸려면 2023·2024 general 두 세트의 해당 `appliesWhen`만 null로 바꾸면 된다.

## 5. ⬜ 마이크로전공 2개 이상 = 전공 이수원칙 충족 (엔진 표현 불가) *(2025~ 신설 규정)*

2025 요람부터 전공 이수원칙이 **"전공심화과정 이수 또는 복수(부)전공을 이수하거나 마이크로전공 2개 이상 이수"** 로 확장됐다(근거: 학사과정 학사운영규칙 2-1-2 제20조3항, **신설 2025.04.03**. 요람 문구가 괄호 단서까지 규칙과 일치).

- 문제: 엔진 `MajorPolicy`는 `minAdditionalMajorCount` **단일 임계값**만 지원한다(`check.ts` — `satisfiesMajorPolicy && satisfied`인 추가전공 개수 ≥ 임계값). "복수전공은 1건, 마이크로전공은 2건"처럼 **유형별로 다른 개수**를 요구할 수 없다.
- 현재 처리(보수적): `am_micro.satisfiesMajorPolicy = false` 유지(2024와 동일). 마이크로전공 2개를 이수한 학생이 실제로는 충족인데 **미충족으로 표시**된다. 반대로 true로 두면 1개만 이수해도 충족으로 잘못 판정되므로, 과소 인정 쪽을 택했다.
- 처리 시 범위: `MajorPolicy`에 유형별 요구 개수(예: `countsByType: { micro_degree: 2 }`) 또는 `satisfiedBy` 활성화. `satisfiedBy` 필드는 현재 **엔진이 읽지 않는다**(선언만 존재) — 이 확장의 자연스러운 자리.
- 관련: `requirement-set-2025-sw.json` / `-2026-sw.json` source.notes, `additional-major-rules-2025/2026-sw.json` am_micro sourceNote.

## 6. ⬜ 마이크로전공 이수학점 규정 상충 (9 vs 12~15)

두 규정이 다른 값을 준다.

| 규정 | 값 |
|---|---|
| 연계전공 운영규칙(3-3-6) 제5조3항 | 연계전공을 마이크로전공으로 이수 시 **9학점 이상** (개정 2023.01.13, 2026판 유지) |
| 학사과정 학사운영규칙(2-1-2) 제23조의2 1항 | 필수과목 포함 **12~15학점**(협약 등 특별 사유 시 9~11). 개정 2026.04.08, 부칙상 **2025학년도 이후 교육과정 신·편입생부터 적용** |

- 현재 처리: 요람 표기값(2025·2026 소융대 통합본 마이크로전공 3종 = 9학점)을 채택하고 `verified:false` 유지.
- 확인 필요: 소프트웨어융합대학 마이크로전공이 "연계전공으로 운영되는 마이크로전공"인지(→9) 일반 마이크로전공인지(→12~15).

## 7. ⬜ 기존 세트(2021~2024)의 학칙 조항 개정일 표기 오류

2021~2024 요건 세트의 `source.notes`·`recognitionCaps[].sourceNote`가 평점표를 `학칙 제46조6항(2026.05.22)`, 인정학점 상한을 `학칙 제45조2항(2026.05.22 개정)`으로 적고 있다. **2026.05.22는 학칙 문서 전체의 최종 개정일**일 뿐 해당 조항의 개정일이 아니다.

- 실제: 제46조6항 = **항 개정 2024.01.18** / 제45조2항 = **항 개정 2024.07.30**.
- 2025·2026 세트는 `(항 개정 YYYY.MM.DD)` 형식으로 이미 정정했다. 2021~2024 4개 세트에 같은 치환을 적용하면 된다(값 변경 없음, 표기만).
- 함께: `regulations.md` §7에 **학칙 제45조11항(재직자전형 선행학습경험 3학점 인정, 신설 2025.04.28)** 행 추가.

## 8. ⬜ AI리터러시(2026 신설 대학필수) 학수번호·평가방식 미확인

2026학년도부터 **AI리터러시 3학점이 전교 공통 대학필수**로 신설됐다(표본 확인: 전자공학·경영학·수학·사학 등 대학필수 소계 26, SW는 아주희망 대체분 -1로 25).

- 특징: **다산학부대학 교양교육과정 편제표에는 없고 각 학과가 개설**하며 과목명이 학과마다 다르다 — 전자·수학 `인공지능프로그래밍`, 경영 `인공지능의 이해`(CCMP113), 소프트웨어학과 `인공지능이해와응용`. 그래서 교양 카탈로그가 아니라 **학과 카탈로그**(`SW-AI-LITERACY`)에 넣었다.
- 미확인: SW 몫의 **학수번호·평가방식**. 2026 SW 요람·소융대 통합본 모두 과목개요에 미수록이라 `codes`가 비어 있다.
- 미확인: 자유전공학부 요람은 AI리터러시 4과목 **택1 + 상호 인정**을 규정하는데(2026학년도_자유전공학부.pdf 비고 3·4), 이 상호 인정이 **일반 학과 학생에게도 적용되는지** 불명 — 적용된다면 `equivalents` 3건을 추가해야 한다.
- 함께: `교양_편제.md`에 2026 AI리터러시 편제 문단 추가 필요(현재 문서는 2025 개편까지만 반영).

## 9. ⬜ 기계공학과 2022 일반과정 영역별교양 영역 구성이 2021과 반대 *(학과 확인 필요)*

기계공학과 요람의 **일반과정** 편성표가 인정하는 영역별교양 3영역이 학번 사이에 뒤바뀐다.

| 학번 | 요람 일반과정 편성표 표기 |
|---|---|
| 2021 | 역사와철학 · **문학과예술** · 인간과사회 |
| 2022 | 역사와철학 · 인간과사회 · **자연과과학** |

- 데이터는 **요람 인쇄대로** 넣었다(`requirement-set-2022-me.json` general의 `area_liberal.areas`).
- 문제: 공과대학은 다산 교양 편제상 **자연과과학이 소속 계열 제외 영역**(`교양_편제.md` 「계열별 제외 영역」)이다. 2022 표기를 그대로 두면 다산 원칙대로 문학과예술을 이수한 2022학번이 **영역별교양 미인정**으로 뜨고, 반대로 자연과과학 이수자는 인정된다. 2021학번과 판정이 정반대가 된다.
- 가설: 2022 요람이 **인증과정 표(자연과과학 포함)를 일반과정에 옮겨 적은 편집 오류**일 가능성이 있다. 인증과정은 ABEEK 지정과목 때문에 자연과과학을 포함하는 게 정상이다.
- 확인 방법: 학과 사무실 또는 2023 요람의 일반과정 표(2023이 2021식으로 돌아갔다면 2022가 오류).
- 처리 시 범위: `requirement-set-2022-me.json`의 general `area_liberal.areas`를 `[hist_phil, lit_art, human_soc]`로 교체 + `data-me.test.ts`의 `generalAreaTrio` 갱신. 인증과정은 건드리지 않는다.

## 10. ✅ 버킷 minCredits 합 = totalCredits 구조에서 초과 이수 오판 *(전 학과 공통)* *(2026-07-24 처리 — R3a)*

모든 요건 세트가 `buckets`의 `minCredits` 합을 `totalCredits`와 정확히 같게 맞춘다(SW·ME 전부). 그런데 전공선택·영역별교양에는 `creditCap`/`overflowTo`가 없다.

- 결과: 전공선택을 초과 이수한 학생(예: 기계 인증과정 전공선택 32 대신 41학점)은 총 이수학점이 128을 넘어도 **일반선택이 그만큼 비어** `일반선택 N학점 부족`으로 졸업 불가 판정을 받았다. 실제 학사에서는 초과분이 잔여 학점으로 흡수되는 게 보통이다.
- 발견: 기계공학과 데이터화 적대적 검증(2026-07-23), engine-behavior 렌즈.
- **처리(개편 결정 ③ — 엔진 암묵 이월, 데이터 무변경)**: `aggregate.ts` buildContext가 일반선택(`group: general_elective`) 외 버킷의 `minCredits` 초과분을 합산해 `AggContext.implicitCarry`로 제공하고, `checkBucket`이 **일반선택 충족 계산에만** 합산한다(`BucketResult.carriedIn`·`notes`). 계약: `bucketEarned`·`totalEarned` 불변(이중 합산 없음) / 명시 `creditCap`·`overflowTo`로 이동한 분은 재합산 안 됨 / `countsTowardTotal:false` 영역 제외. 이월이 실제로 쓰일 때만 "…초과 N학점이 잔여 학점으로 집계됐어요" 사유를 요약·요건 탭에 표시. 전 세트(미래 데이터 포함)에 적용되며 세트별 creditCap 살포안(a)은 기각.
- 검증: `implicit-carryover.test.ts` 8건(합산·부분 충족·다중 출처·이중 계산 가드·미산입 제외·역방향 금지·예정 컨텍스트) + `data-me.test.ts` B-06d 기계 인증 전공선택 41학점 재현(2021·2022) → 졸업 가능.

## 11. ⬜ 엔진 하드코딩 안내 문구 2건이 상황에 따라 사실과 다름

`src/engine/aggregate.ts`·`src/data/merge.ts`가 사용자 문구를 하드코딩해, 새 학과 구조에서 틀린 설명이 나온다.

| 위치 | 현재 문구 | 틀리는 상황 |
|---|---|---|
| `merge.ts:158` | "영역별교양 미인정 — {영역}: **소속 계열 영역이라** 일반선택으로 집계" | 기계 **인증과정**은 문학과예술을 빼는데, 이유가 소속 계열이 아니라 **ABEEK 지정 영역이 아니어서**다 |
| `aggregate.ts:146` | "초과 N학점은 **일반선택으로** 이월됐습니다" | `overflowTo`가 일반선택이 아닌 세트에서 거짓말이 된다 |
| `aggregate.ts` 이월 경고 | `creditCap: 0`일 때 "최대 0학점까지 인정"으로 읽힘 | 기계 인증과정 학부연구프로젝트(교양 1학점 인정)가 **학점이 사라진 것처럼** 보인다 |

- 처리 시 범위: 사유 문구를 세트 데이터(`courseGroups[].label`/신설 `note` 필드)에서 읽게 하거나 중립 문구로 교체. 판정에는 영향 없고 표시만 바뀐다.

## 12. ⬜ 기계공학과 공학인증 폐지 — 심화교육과정 세트 부재

학칙 부칙(2024.01.18.) 제3조2항: 기계공학전공 공학교육인증제 포기가 **2025년 3월부터 전 학생에게 적용**되고, 2025년 8월 졸업생부터는 **심화교육과정 또는 일반과정**을 선택한다. 2021·2022학번도 소급 대상이다.

- 현재: `rs_{2021,2022}_me_accredited` 세트가 온보딩에 정상 노출된다. 세트 `source.notes`에 폐지 사실과 "현재 재학생은 일반과정을 쓰되 학과 확인" 안내를 넣었지만, **온보딩 화면에는 경고가 없다**.
- 대체 경로인 **심화교육과정 세트가 없다** — 심화과정 편제는 2024학년도 이후 요람에 나오므로, 2021·2022 요람만으로는 만들 수 없다.
- 처리 시 범위: (a) 2024 이후 기계공학과 요람에서 심화교육과정 편제를 확인해 `rs_{2021,2022}_me_advanced`를 소급 생성할지 판단, (b) 폐지된 과정 세트에 표시용 플래그(예: `source.deprecated`)를 두고 온보딩에서 안내. (b)는 다른 학과의 폐지 과정에도 재사용된다.

## 13. ⬜ 다산 교양 카탈로그 학수번호 교차 부여 7건 *(요람 대조 필요)*

R2 범용 무결성 스위트(A-07 학수번호 중복 검사) 도입 과정에서 확인. GE 카탈로그에서 같은 학수번호가 서로 다른 courseKey 여럿에 부여돼 있고, **6개년(2021~2026) 일관**이라 추출 편차가 아니라 편제 문서 표기로 보인다.

| 학수번호 | 공유 과목 | 성격 |
|---|---|---|
| COLT103 | 명저읽기 연작 1~3 | 연작 강좌 — 자연스러움 |
| AJOU102 | 아주강좌 시리즈 1~2 (2021~2024만) | 연작 강좌 — 자연스러움 |
| CART108 | 현대드라마와공연 · 자유드로잉연습 | **서로 다른 과목 간 공유 — 재확인 필요** |
| SCI114 | 과학기술과법 · 에너지와사회 | 〃 |
| CKOR104 | 창의적사고와표현 · 창의적글쓰기 | 〃 (개명 병존일 수 있음) |
| CSOC102 | 다문화사회이해 · 일가족사회복지 | 〃 |
| CMAT108 | 데이터수학기초 · 드론비즈니스모델 (2022~) | 〃 |

- 현재: `data-invariants.test.ts`의 허용 목록(`DUPLICATE_CODES`)에 관측값 그대로 고정 — 새 중복이 생기거나 기존 중복이 사라지면 테스트가 잡는다.
- 판정 영향: 학수번호로 과목을 검색하면 후보 복수(ambiguous)로 떠서 사용자가 고른다. courseKey 참조 원칙 덕에 판정 자체는 안전.
- 처리 시: 다산 편제 문서에서 7건을 대조해 (a) 실제 공유면 유지, (b) 추출 오류면 정정 + 허용 목록 갱신 + 스냅샷(R0) 갱신.

## 참고. creditBreakdown 설계/실습 분류 오차 (2021~2024 잔존)

2021~2024 카탈로그 일부 과목의 `creditBreakdown`이 요람 편성표의 설계(design) 학점을 실험실습(lab)으로 분류한다(IT집중교육 design4→lab4, 이론2·설계1 과목 다수, SW캡스톤디자인 lab6→design6 등). **엔진·앱 어디서도 creditBreakdown을 읽지 않아**(types.ts 타입 정의에만 존재) 판정·학점 계산에 영향 0이고 `credits`(합계)는 전부 정확하다.

- **2025·2026은 정확하다.** 데이터화 시 `-layout` 텍스트의 **열 x좌표로 이론/설계/실험실습을 분류하고, 요람 인쇄 소계(2025 이론98·설계28.5·실습45.5 / 2026 이론116·설계28.5·실습45.5)와 산술 대조**해 확정했다. 두 해 모두 소수점까지 일치한다(테스트 A-24 / A-23이 회귀 가드).
- 2021~2024도 같은 방법으로 정정 가능하다. 순수 표시 메타데이터라 우선순위는 낮지만, 착수하면 기계적으로 끝난다.

---

*미확정 slug 확정·동기화는 여기서 관리하지 않음 — `데이터화_진행률.md` 주의사항 7과 `naming.md` 시드 문단이 관장.*
