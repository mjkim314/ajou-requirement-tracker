# 개선 방향 제안 — 데이터 생산 라인과 커버리지 전략

작성 2026-07-24. 이 문서의 수치는 이날 리포에서 직접 측정한 값입니다.

**문제의식**: 앱 코어(1~7단계)는 끝났는데, 데이터화가 주전공 226건 중 14건(6%)입니다.
현재 방식은 1건마다 완전한 JSON 두 벌 + 수백 줄 테스트 + 3곳 수동 등록을 요구합니다.
이 속도로는 남은 212건 + 연계전공 약 30건에 세션 100회 이상이 듭니다.
**엔진과 앱은 문제가 아닙니다. 데이터 생산 라인과 "전부 다 만든다"는 목표 설정이 병목입니다.**

---

## 1. 요약 — 다섯 가지 제안

| # | 제안 | 한 줄 요약 | 효과 | 난이도 |
|:--:|---|---|---|:--:|
| **A** | 소스·산출물 분리 + 상속 모델 | 학번·과정·학과 3축의 90%+ 중복을 "베이스 + 패치"로 작성. 런타임 JSON은 지금과 동일하게 빌드로 생성 | 같은 학과 다음 학번 비용 60~80%↓, 전역 정정 N배→1배 | 중 |
| **B** | 테스트 생성 | 학번별 400~900줄 수제 테스트 → 범용 무결성 스위트 + 건당 "요람 기대값 명세" 수십 줄 | 건당 테스트 작성 소멸, 검증 자동화 | 중 |
| **C** | 등록 자동화 | `index.ts`·`DATA_BUNDLES` 손 등록 → 파일명 규약 기반 자동 매니페스트 | "등록 누락 → 조용한 오동작" 오류 계급 소멸 | 하 |
| **D** | 커버리지 전략 전환 | "226건 전부 검증" → 티어제(검증 프리셋 / 초안 / 뼈대 생성기) | 검증 대상을 ~1/3로, 롱테일은 편집기로 해결 | 하(정책) |
| **E** | 전역 결정 선처리 | 백로그 4·10·11 같은 전 세트 공통 결정을 데이터 추가 재개 **전에** 처리 | 정정 비용이 세트 수에 비례 증가하는 것을 차단 | 하 |

**권장 조합**: C → B(범용 스위트) → E → A(파일럿) → D(정책 확정) 순.
A~C는 순수 내부 개선이라 되돌릴 수 있고, D만 제품 방향 결정이 필요합니다(§11).

---

## 2. 진단 — 작업 1건의 비용이 어디로 가는가

### 2.1 현재 1건(학과 × 학번)의 비용 해부

`yoram-extract` 워크플로 기준:

| 단계 | 산출물 | 성격 |
|---|---|---|
| PDF 슬라이스·추출 | 텍스트 | 본질 비용 (줄이기 어려움) |
| 카탈로그 작성 | `catalog-*.json` 74~79과목 **완전본** | **70~76%가 전 학번 복사** |
| 요건 세트 작성 | 과정별(심화/일반) **완전본 2벌** | **학번 간 95%·과정 간 94% 복사** |
| 교양 버킷 채우기 | 세트 안 4~5개 버킷 | **학과 간 바이트 단위 동일 복사** |
| 검증 테스트 작성 | `data-*.test.ts` 415~898줄 | **절반 이상이 학번 간 복사** |
| 등록 | `index.ts` + `DATA_BUNDLES` + `ajou-departments.ts` 3곳 | 수동. 누락 시 조용한 오동작 |
| 적대적 검증 | 정정·백로그 항목 | 본질 비용 (품질의 원천) |

### 2.2 중복 실측 (2026-07-24)

| 축 | 측정 | 결과 |
|---|---|---|
| **학번 간** | requirement-set 2023→2024 SW, 리프 값 비교 | 622개 중 **95% 동일** |
| **학번 간** | 카탈로그 인접 학번 항목 바이트 비교 | SW 2023→2024 **70%**, 2025→2026 **75%**, ME 2021→2022 **76%** 완전 동일¹ |
| **과정 간** | 2024 SW advanced vs general, 리프 값 비교 | **94% 동일** |
| **학과 간** | 2021 SW vs ME의 교양 버킷(english·writing·ajou_character·area_liberal) | 4개 전부 **바이트 단위 완전 동일** |

¹ 2021→2022 SW만 17%로 낮은데, 이는 2022 작업에서 소급 정정(캡스톤 6학점 등)과 메모 보강이 몰렸기 때문. 파이프라인이 안정된 뒤로는 70%대가 정상치.

즉 데이터 1건의 실질 정보량은 "**이번 학번에 바뀐 것**"뿐인데, 작성·검증·정정은 항상 **완전본 단위**로 하고 있습니다.
백로그 9번(기계 2022 영역 구성) 같은 사고도 정확히 이 "손 복사" 과정에서 났습니다.

### 2.3 구조적 문제 4가지

1. **복제 배수 효과** — 전역 정정 1건이 세트 수만큼 비싸집니다. 백로그 10번은 이미 "SW 6학번 × 2과정 + ME 2학번 × 2과정 전부에 영향"이라고 적혀 있습니다. 226건이 되면 같은 정정이 28배 더 비쌉니다.
2. **테스트 선형 증가** — 데이터 테스트만 3,888줄(전체 632건). `data-2024-sw.test.ts`의 A-01~A-05 같은 무결성 검사는 매 학번 복사되는 범용 검사입니다. 226건이면 테스트 코드만 수만 줄이 됩니다.
3. **손 등록 3곳** — `src/data/index.ts`가 8묶음에 이미 194줄. 스킬 문서 스스로 "하나라도 빠지면 테스트는 통과해도 앱이 조용히 오동작"이라고 경고하는 구조를 226번 반복하게 됩니다.
4. **번들 전량 포함** — 모든 JSON이 정적 import라 초기 번들에 들어갑니다. 교양 카탈로그만 84~100KB × 6. 226건이면 수 MB로 향합니다.

---

## 3. 제안 A — 소스와 산출물 분리: 상속(패치) 모델

**핵심**: 사람(과 LLM)이 작성하는 **소스 포맷**과 앱이 읽는 **런타임 포맷**을 분리합니다.
런타임 포맷(`src/data/*.json`)은 **한 글자도 안 바뀝니다**. 엔진·앱·스토리지·공유 화이트리스트 전부 무변경.

```
data-src/                          ← 사람이 편집하는 소스 (신설)
  ge/buckets-2021.json …           ← 교양 공통 버킷 조각 (학번별, 전 학과 공유)
  sw/base.json                     ← 학과 베이스 (첫 학번 완전본)
  sw/2022.patch.json               ← 그 해 바뀐 것만
  sw/2024.patch.json
  sw/tracks.patch.json             ← 과정 간 차이 (심화 vs 일반)
      │
      ▼  scripts/build-data.mjs (id/courseKey 키 병합)
src/data/catalog-2024-sw.json      ← 지금과 동일한 완전본, 커밋 유지
src/data/requirement-set-2024-sw.json
```

실제 사례로 보면, 2024 SW의 실질 변경은 "전공선택에서 2과목 제외 + 인증 문구 1건"이 전부입니다:

```jsonc
// data-src/sw/2024.patch.json — 이것이 2024 작업의 전체 산출물이 된다
{
  "extends": "2023",
  "catalog": { "$remove": ["SW-NET-SEC", "SW-SYS-SW"] },
  "sets": { "*": { "buckets": { "major_elective": { "note": "…전선 소계 176→170" } } } }
}
```

설계 원칙:

- **병합은 빌드 타임에만.** "과정 분기는 완전본을 각각 담는다(병합 로직 금지)"는 기존 규칙은 런타임 단순성을 위한 것이었고, 그 목적은 그대로 지켜집니다 — 앱은 여전히 완전본만 봅니다.
- **병합 규칙은 단순하게.** 객체는 깊은 병합, 배열은 id(버킷·choiceGroup)/courseKey(카탈로그) 키 병합, 삭제는 `$remove` 명시. 병합기 자체는 100줄 안팎의 순수 함수로 만들고 단위 테스트를 둡니다.
- **마이그레이션 게이트**: 기존 8묶음을 소스 포맷으로 역이식한 뒤, 재생성 결과가 커밋된 JSON과 **바이트 동일**해야 통과. 기존 632개 테스트가 안전망이 됩니다.
- **교양 버킷은 조각 공유**: 지금은 `교양_편제.md`(문서)를 보고 각 세트에 손으로 복사합니다. 조각(`ge/buckets-{학번}.json`)을 include하고 학과 편차(기계 인증 지정과목 등)만 그 위에 패치하는 방식으로 바꾸면, 백로그 9번류의 복사 사고가 구조적으로 줄어듭니다.

얻는 것:

| 상황 | 지금 | 상속 모델 |
|---|---|---|
| 같은 학과 다음 학번 | 완전본 재작성 + 전체 재검증 | **패치 작성 + 델타만 검증** (95% 중복 실측 근거) |
| 과정 추가 (심화/일반/인증) | 완전본 한 벌 더 | 패치 몇 줄 (94% 중복) |
| 전역 정정 (백로그 10 등) | 세트 수만큼 반복 수정 | **베이스/조각 1곳 수정 → 재생성** |
| 리뷰 | 24KB JSON diff | 사람이 읽는 "이 해에 바뀐 것" 패치 |

리스크와 완화:

- 병합기 버그 → 바이트 동일 게이트 + 병합기 단위 테스트 + 기존 테스트 전체가 산출물을 계속 검증.
- "소스와 산출물 이중 관리" 부담 → 산출물은 손대지 않는 규칙(CI에서 재생성 일치 검사 1건이면 강제 가능).
- 학번 개편이 큰 해(예: 2025 교양 대개편)는 패치가 커짐 → 그런 해는 새 베이스로 끊으면 됩니다(`extends` 없는 파일). 모델이 강제하지 않습니다.

---

## 4. 제안 B — 테스트를 쓰지 않고 생성

세 가지를 바꿉니다.

**B-1. 범용 무결성 스위트** — `data-*.test.ts`마다 복사되는 A-계열 검사(courseKey 유일성, requiredCourses 존재, linkedTo 유효, 영역 정합, `group:'free'` 폴백 1개 …)를 **등록된 전체 번들을 순회하는 파일 1개**로 통합합니다. 새 데이터를 추가하면 테스트 코드 0줄로 즉시 전 검사가 적용됩니다. 기존 학번별 파일에서는 중복 절을 점진 삭제하고, 학과 고유 사실(기계 인증 6과목 승격 등)만 남깁니다.

**B-2. 요람 기대값 명세** — 건마다 `expected/{학번}-{slug}.json`을 둡니다. 내용은 요람 인쇄면에서 그대로 읽는 소계들:

```jsonc
{ "totalCredits": 140, "buckets": { "major_required": 37, "major_elective": 33, "…": 0 },
  "majorRequiredCount": 11, "catalogCount": 74,
  "creditBreakdown": { "theory": 116, "design": 28.5, "lab": 45.5 } }
```

범용 비교기 테스트 1개가 생성된 세트·카탈로그를 이 명세와 대조합니다. 이미 검증법으로 확립한 "열좌표 × 소계 대조"(2025·2026에서 소수점까지 일치 확인)를 **문서 속 절차에서 실행되는 코드로** 승격하는 것입니다. 요람을 읽는 수고는 그대로지만, 읽은 결과를 테스트 코드가 아니라 데이터로 적게 됩니다. 적대적 검증의 산술 대조 절반이 자동화됩니다.

**B-3. 판정 전이 시나리오 자동 생성** — 세트 자체에서 합성 성적표를 만들 수 있습니다(모든 requiredCourses 이수 + choiceGroup 충족 + 남는 학점 채움 → `graduatable`, 전필 1개 제거 → `not_graduatable`, 재학 중 상태 → `graduatable_after_current`). 코호트마다 수제 대표 성적표를 만드는 대신 생성기 1개 + 대표 학과(SW·ME)만 수제 유지.

효과: 건당 테스트 산출물이 "TS 400~900줄" → "기대값 JSON 수십 줄"이 됩니다. 테스트 총량 증가 곡선이 상수로 꺾입니다.

---

## 5. 제안 C — 등록 자동화와 지연 로딩

**C-1. 매니페스트 자동화** (즉시, 반나절): 파일명 규약이 이미 엄격하므로(`catalog-{학번}-{slug}.json` …) Vite의 `import.meta.glob('./data/*.json', { eager: true })`으로 파일을 열거하고, 이름을 파싱해 `requirementSetRegistry`·`DATA_BUNDLES`를 **자동 구성**합니다.

- `index.ts` 194줄의 수동 import·수동 레지스트리, `dataSources.ts`의 수동 매핑이 사라집니다.
- "등록 3곳 중 하나 누락 → 폴백 카탈로그 → 전 과목 미매칭" 오류 계급이 **구조적으로 소멸**합니다.
- `ajou-departments.ts`와 세트 `department` 철자 일치는 매니페스트 검증 테스트 1건으로 상시 확인.

**C-2. 지연 로딩** (번들이 1MB를 넘보면): 같은 glob에서 `eager: false`로 바꾸고 선택된 학과·학번 번들만 비동기 로드. 데이터 로드 경로가 이미 `dataSources.ts` 한 곳이라 전환 지점도 한 곳입니다. 지금 당장은 불필요하고, 임계 도달 시점에 켭니다.

---

## 6. 제안 D — 커버리지 전략 전환: 전부 만들지 않는다

가장 큰 지렛대는 기술이 아니라 목표 재설정입니다. **"226건 전부 요람 수준 검증"은 1인 운영에서 유지 불가능한 약속**이고, 앱에는 이미 그 약속을 대체할 자산(요건 편집기, clone-on-edit, 공유 백업, 전 학과 공통 교양 기반)이 있습니다.

| 티어 | 대상 | 제공 수준 | 건당 비용 |
|:--:|---|---|---|
| **T1 검증 프리셋** | 수요 상위 학과 (예: 소융대 5 + 공대 주요 + 경영·전자 등 10~15개) × 전 학번 | 지금과 동일: 적대적 검증 + 기대값 명세 + `verified:true` | 지금의 새 라인 비용 |
| **T2 초안 프리셋** | 나머지 요람 보유 학과 | 추출 + 기대값 산술 대조까지만. **`verified:false`를 온보딩에서 명시** ("요람과 대조해 확인하세요") | T1의 절반 이하 |
| **T3 뼈대 생성기** | 프리셋 없는 학과·신설 전공 | 온보딩에서 (학번, 계열) 입력 → 교양 공통 버킷(완비됨) + 학칙 전역값 채운 **뼈대 세트** 생성 → 사용자가 요람 보고 전공 숫자만 편집기로 채움 | 0 (1회 구현) |

- T3가 롱테일의 정답입니다. 교양+전역값이 세트의 절반이므로 사용자가 채울 것은 전공 버킷 몇 개 숫자뿐이고, 편집기는 이미 있습니다. "참고용" 고지 원칙(CLAUDE.md)과도 일치합니다.
- **수요 신호**: 서버 없이도 앱의 "우리 학과가 없어요" 지점에 GitHub 이슈 템플릿 링크를 달면, 요청 순서대로 T2→T1 승격 대상을 고를 수 있습니다.
- (선택) **커뮤니티 세트**: 공유 내보내기 파일을 받는 `community-sets/` 절차를 만들고 CI가 범용 스위트(B-1)로 검증. 각 학과 사용자가 자기 학과를 채우는 경로가 열립니다.
- 연계전공 약 30건은 기본 T2로 두고, 수요가 확인된 것만 T1 승격.

이 정책이면 **적대적 검증까지 가는 건수가 226 → 약 60~90건**으로 줄고, 나머지는 훨씬 싼 경로로 커버됩니다.

---

## 7. 제안 E — 전역 결정을 먼저 끝낸다

`데이터_정정_백로그.md`의 미결 항목 중 **전 세트에 영향을 주는 결정**은 세트가 8묶음인 지금 처리하는 것이 226건일 때보다 수십 배 쌉니다. 특히 신규 데이터가 같은 함정을 계속 복제 중입니다.

| 백로그 | 내용 | 성격 |
|:--:|---|---|
| 4 | 일반과정 산학프로젝트 인증 미검사 | 정책 판단 필요 (전 코호트 공통) |
| 10 | 버킷 초과 이수 → 일반선택 부족 오판 | 엔진/데이터 전역 결정. **모든 신규 세트가 함정 복제 중** |
| 11 | 하드코딩 안내 문구 2건 | 소폭. 새 학과 구조마다 거짓말 추가됨 |
| 5 | 마이크로전공 2개 = 이수원칙 충족 | 엔진 확장 (`satisfiedBy` 활성화) |

데이터화 재개 전에 4·10·11을 먼저 결정·반영할 것을 권합니다. A(상속 모델)가 들어오면 이런 정정의 반영 비용 자체도 "베이스 1곳"으로 떨어져 상호 보강됩니다.

---

## 8. 바꾸지 않는 것

- **계산 엔진** — 순수 함수, 검증 완료. 백로그가 요구하는 소폭 확장(5·10) 외 재설계 없음.
- **런타임 데이터 포맷·스키마** — `RequirementSet`/`CatalogEntry` 그대로. A는 소스 계층 신설이지 포맷 변경이 아님.
- **저장 계층·백업·공유** — 그대로.
- **UI·온보딩 흐름** — T3 뼈대 생성기 진입점 추가 외 그대로.
- **아키텍처 4원칙** (순수 엔진 / Storage Adapter / 숫자 하드코딩 금지 / courseKey) — 전부 유지. 이 제안들은 4원칙을 어기지 않고 그 위에 얹는 것.
- **원본 PDF 품질 문제**(병합셀, 개명, 통합본 슬라이스)는 구조로 없앨 수 없습니다. 줄이는 것은 그 주변의 반복 비용입니다.

---

## 9. 적용 순서 (각 단계가 독립적으로 되돌릴 수 있음)

| 단계 | 내용 | 규모 | 게이트 |
|:--:|---|:--:|---|
| 0 | 현 8묶음 산출물 스냅샷(해시) 고정 테스트 | 반나절 | — |
| 1 | **C-1** 매니페스트 자동화 | 반나절~1일 | 기존 테스트 전체 통과 + 매니페스트 검증 |
| 2 | **B-1** 범용 무결성 스위트 (기존 파일 정리는 점진) | 1~2일 | 커버 확인 후에만 기존 절 삭제 |
| 3 | **E** 전역 결정 3건 (4·10·11) | 항목당 0.5~1일 | 사용자 정책 확인 |
| 4 | **A** 파일럿: SW 6학번 역이식 → 바이트 동일 게이트 → ME 확장 | 2~4일 | 재생성 = 커밋본 바이트 동일 |
| 5 | **B-2·B-3** 기대값 명세 + 시나리오 생성기 — 이후 신규 건은 새 라인만 사용 | 1~2일 | 기존 SW/ME로 명세 역검증 |
| 6 | **D** 티어 확정(§11 결정) + T3 뼈대 생성기 | 2~3일 | — |
| 7 | (임계 도달 시) C-2 지연 로딩 | 1일 | — |

1~2가 끝나면 그 시점부터 신규 데이터 건의 등록·테스트 비용이 즉시 줄므로, 데이터화를 완전히 멈출 필요는 없습니다. 다만 **3(전역 결정) 전에는 신규 세트를 늘리지 않는 것**을 권합니다.

---

## 10. 예상 효과 (추정치)

| 항목 | 지금 | 개선 후 |
|---|---|---|
| 신규 학과 첫 학번 | 기준 | 테스트 작성·등록 소멸로 **~30~40%↓** |
| 같은 학과 다음 학번 | 완전본 재작성 | 패치+델타 검증 **~60~80%↓** (중복 95% 실측 근거) |
| 과정(트랙) 추가 | 완전본 한 벌 | 패치 몇 줄 |
| 전역 정정 | 세트 수 × 수정 | 1곳 수정 + 재생성 |
| 테스트 코드 | 건당 400~900줄 누적 | 상수(범용 스위트) + 건당 기대값 수십 줄 |
| 등록 실수 가능성 | 3곳 수동 | 0 (파일명 규약) |
| 검증 완료 목표 건수 | 226 + 30 | **~60~90 (T1)** + 초안 + 생성기 |

추출·요람 대조라는 본질 비용은 남습니다. 이 제안은 그 **주변의 복사·보일러플레이트·반복 검증**을 없애는 것입니다.

---

## 11. 결정이 필요한 것 (사용자 판단)

1. **티어 커트라인** — T1(전 검증) 학과를 어디까지로? (제안: 소융대 5 + 기계·전자·경영 + 수요 신호 반영, 나머지는 T2/T3)
2. **T2 초안 프리셋 노출 여부** — `verified:false` 라벨을 달고 온보딩에 노출할지, 숨기고 요청 시만 제공할지. (제안: 노출. "참고용" 고지 원칙과 일치하고, 없는 것보다 낫다)
3. **백로그 4·10의 정책** — 일반과정 인증 게이팅(pick 1 활성화?), 초과 이수 처리(overflowTo vs warning 강등). 이건 원래부터 사용자 몫으로 남아 있던 판단.
4. **A의 적용 범위** — 신규 건만 새 라인으로 갈지, 기존 8묶음도 역이식할지. (제안: 역이식. 전역 정정 이득이 크고 바이트 동일 게이트로 위험이 낮다)
5. **커뮤니티 세트 수용 여부** — 외부 기여를 받을지. (급하지 않음, 6단계 이후)

---

*이 문서는 제안입니다. 채택 항목이 정해지면 `TASKS.md`에 단계로 편입하고 이 문서에 결정 사항을 기록합니다.*
