# 작업 순서

각 단계는 **완료 기준을 만족해야 다음으로 넘어간다.** 앞 단계를 건너뛰고 화면부터 만들지 말 것.

전제는 `CLAUDE.md`에 있다. 특히 아키텍처 4대 규칙을 먼저 읽을 것.

**현재 진행** — 0·1·2·3·4·5·6·7 **전부 완료**(앱 골격 / 엔진 68건 / 데이터 2021·2022 SW / 저장·백업 / 온보딩 마법사 / 요약·과목 탭 / 요건 탭 + 편집기 / **백업 리마인더 + 설정**). 전체 284 테스트 통과·빌드 성공. **핵심 로드맵 종료.** 그 외 학과·학번 데이터는 `yoram-extract` 스킬로 확장한다.

**다음 작업** — 데이터 생산 라인 개편 **R0~R7** (이 문서 하단 「개편 로드맵」). 근거·측정치는 `개선_방향_제안.md`. **R3 완료 전에는 신규 학과·학번 데이터화를 착수하지 않는다.**

> **개편 진행** — **R0·R1·R2 완료**(2026-07-24, 커밋 4건: 스냅샷 30파일 고정 / 매니페스트 자동 등록 + 정합 29건 / 범용 무결성 스위트 346건 / 학번별 중복 절 -532줄·커버 대조표). 전체 **904 테스트**·타입체크·빌드 통과. 다음은 **R3**(전역 결정 반영 — R3a 암묵 이월부터).

---

## 0. 앱 골격 + 디자인 토큰

**목표** React 앱이 뜨고, 시안 톤의 셸(헤더·탭) 위에서 엔진·데이터가 연결된 상태. **3단계부터의 모든 UI가 이 위에 올라간다.**

> 원칙상 맨 앞 단계지만, 1·2는 화면 없이 되는 작업이라 먼저 끝냈다(헤드리스 엔진+데이터). **UI가 붙는 3단계 전에 지금 여기부터 착수한다.** 현재 repo는 vitest만 있고 React·Vite·Tailwind가 없다.

**만들 것**

```
index.html
vite.config.ts · tailwind.config.ts · postcss.config.js
src/
  main.tsx           React 진입점
  App.tsx            헤더 + 탭 셸(요약 · 과목 · 요건 · 설정)
  theme/tokens.css   CLAUDE.md 디자인 토큰 → CSS 변수 (또는 tailwind.config)
  app/               앱 상태(프로필·과목) 컨텍스트/훅, evaluate() 호출 지점
package.json         react · react-dom · vite · @vitejs/plugin-react · tailwindcss 등 추가, dev·build·preview 스크립트
```

**핵심 규칙**

- **디자인 토큰은 한 곳에만.** CLAUDE.md의 색·라운드(20/13/9)·그림자·폰트를 Tailwind 설정 또는 CSS 변수로 **한 번만** 정의하고 컴포넌트에서 하드코딩 금지
- 폰트 Pretendard, 아이콘 Material Symbols Rounded 로드(CLAUDE.md)
- **시안에서 가져올 것**: 색·타이포·여백·라운드·컴포넌트 생김새·정보 배치. **버릴 것**: 인라인 스타일, `<x-dc>`/`<helmet>` 태그, `support.js`, 2400px 캔버스 — 절대 제품에 포함 금지
- **상태 색은 단독 사용 금지** — 아이콘·텍스트를 항상 병기(색맹 대응)
- 엔진·데이터는 이미 있으니 **import해서 연결만** 한다. 이 단계에선 상태를 메모리에 둬도 되고, 영속화는 3단계 Storage Adapter가 맡는다(컴포넌트에서 localStorage 직접 호출 금지)

**완료 기준**

- `npm run dev` → 헤더 + 빈 탭 셸이 시안 톤으로 렌더되고 탭 전환이 된다
- 앱 코드에서 `evaluate()`를 한 번 호출해 결과가 화면/콘솔에 나온다(엔진 연결 확인)
- `npm run build` 성공, 기존 `npm run typecheck && npm test` 여전히 통과

---

## 1. 계산 엔진

**목표** 화면 없이 졸업 가능 여부를 판정할 수 있는 순수 함수 모듈.

**만들 것**

```
src/engine/
  types.ts        도메인 타입
  normalize.ts    과목명·학수번호 → courseKey
  resolve.ts      영역 귀속 결정
  aggregate.ts    학점 집계, 상한·이월
  check.ts        6축 판정
  evaluate.ts     진입점 — evaluate(input) → result
```

**핵심 규칙**

- `evaluate()`는 **인자로 받은 것만** 사용한다. import한 전역 상태, `Date.now()`, localStorage 접근 금지
- 판정해야 하는 6축: 총 학점 / 영역별 학점 / 필수 과목 / 평점 / 비교과 / 전공 이수원칙
- 영역 귀속 우선순위: **사용자 지정 → 요건 세트 지정 → 카탈로그 기본값 → 일반선택**
- 판정 결과는 3단계: `졸업 가능` / `이번 학기 완료 예정` / `졸업 불가`

**주의할 것**

- **택1 그룹** — 수학은 3과목 + (확률및통계2 또는 선형대수1). 기초과학은 택1이 3겹이고 실험 과목이 본과목과 연동됨
- **영역별교양** — 학점만이 아니라 "서로 다른 3개 영역에서 각 1과목"
- **상한·이월** — 현장실습 과목군은 전공선택으로 6학점까지만. 나머지는 일반선택으로 이월
- **중복 인정** — 복수전공 6학점, 부전공 3학점 상한. 상한 이내에서 **합계를 최대화하는 조합**을 고를 것. 큰 것부터 담으면 학생이 손해 본다 (4·3·3에서 상한 6이면 3+3=6이 정답, 4 단독이 아님)
- **재수강** — 최신 성적만 평점 반영, 이전 기록은 보존
- **F** — 이수학점에서 제외하되 평점에는 포함

**완료 기준**

- `설계명세서_v0.2` 10장의 테스트 45건이 통과한다
- 요건 세트를 바꿔 넣어도 엔진 코드 수정 없이 동작한다

---

## 2. 프리셋 + 카탈로그

**목표** 엔진이 씹을 실제 데이터.

**만들 것**

```
src/data/
  requirement-set-2021-sw.json    심화 + 일반 2종
  catalog-2021-sw.json            교과목 카탈로그
```

원본은 `2021요람_소프트웨어학과.pdf`, 정리된 값은 `설계서_v1.0` 3장과 `설계명세서_v0.2` 부록 A·B에 있다.

**주의할 것**

- **학수번호를 기본키로 쓰지 말 것.** 요람 안에서 SCE411이 모델링시뮬레이션과 고급컴퓨터구조 양쪽에 붙어 있고, 자료구조가 SCE205와 SCE202로 두 번 나온다. `courseKey`를 따로 만들고 학수번호는 `codes[]` 배열로 넣는다
- 표기 흔들림(해외인턴십/해외인턴쉽, 컴퓨터프로그래밍/컴퓨터프로그래밍및실습)은 `aliases[]`로 흡수
- **부전공 전공학점은 36으로 넣고 `"verified": false`를 달 것.** 요람 표가 모순돼 있어 학과 확인 대기 중

**완료 기준**

- 1단계 엔진에 넣어 2021 SW 심화·일반 양쪽이 정상 판정된다

**현재 상태 · 확장** — 2021·2022 SW 완료(각각 심화·일반 2종, `src/data/`에 등록). 다른 학과·학번은 `yoram-extract` 스킬로 요람 PDF에서 추출한다. 학과 무관 수치(평점표·최저평점·추가전공 학점·어학인증 등)는 `기타 참고 자료` 규정으로 교차검증(스킬 `references/regulations.md`), 대학·학과 명칭/소속 변경은 스킬 `references/naming.md`의 이력을 따른다.

**교양은 학과 카탈로그에 넣지 않는다.** 다산학부대학 교양은 `catalog-{연도}-ge.json`(전 학과 공유)에 있고,
`src/data/merge.ts`가 학과 카탈로그와 **학번에 맞는 교양**을 합쳐 앱에 공급한다. 학과 세트는 교양 버킷·영역만
`교양_편제.md`에서 가져와 선언하면 되고, 계열 제외 영역(이공계=자연과 과학)은 세트의 `area_liberal.areas`에
인정 영역만 적으면 자동으로 처리된다(제외 영역 과목은 일반선택으로 집계 + 화면에 "영역별교양 미인정" 표시).

---

## 3. 저장 계층 + 백업·복원

**목표** 데이터가 안 날아가는 상태. **화면보다 먼저 한다.**

**만들 것**

```
src/storage/
  adapter.ts      읽기·쓰기·삭제. localStorage 접근은 여기서만
  backup.ts       내보내기 / 불러오기
  schema.ts       버전 검증·마이그레이션
```

**저장 키 · `updatedAt`** — 키 목록(`profile`·`courses`·`noncurricular`·`reqsets:custom`·`settings`)과 각 덩어리에 `updatedAt`을 넣는 규칙은 `CLAUDE.md` 참조. 지금 안 써도 반드시 넣는다(나중 동기화용).

**내보내기**

- 파일 다운로드 방식. 파일명 `ajou-grad-{학번}-{YYYYMMDD}.json`
- 2종을 구분한다
  - `ajou-grad-backup` — 개인 전체 (프로필·과목·비교과·커스텀 세트)
  - `ajou-grad-reqset` — 요건 세트만 (공유용, **개인 데이터 절대 미포함**)
- `type` 필드로 구분해서, 공유 불러오기에 개인 백업이 들어오면 거부

**불러오기**

- 파일 선택 → 검증 → **미리보기** → 확인 → 적용
- 미리보기에 표시: 백업 일시, 학번·학과, 과목 건수
- 덮어쓰기 전에 **현재 데이터를 먼저 백업받겠냐고 제안**
- 스키마 버전이 앱보다 높으면 거부하고 업데이트 안내

**저장 실패 처리**

- 용량 초과·시크릿 모드 등으로 저장 실패 시 **조용히 넘어가지 말 것**
- **입력값을 화면에서 지우지 않는다.** 사용자가 다시 입력하지 않아도 되게
- 앱 진입 시 저장소 사용 가능 여부를 감지해서, 불가하면 경고를 띄운다

**완료 기준**

- 내보내고 → localStorage를 비우고 → 불러오면 원래 상태로 복원된다
- 시크릿 모드에서 열어도 앱이 죽지 않고 경고가 뜬다

---

## 4. 온보딩  ✅ 완료

> **상태: 완료.** `src/app/onboarding/`에 마법사 구현(시작→내 정보→요건 세트→추가 전공→예외/학기→요건 확인→완료). 순수 로직은 `model.ts`(+테스트), 학과 큐레이션은 `src/data/ajou-departments.ts`. 추가전공 편집값은 `profile.additionalMajors[].rule` 인라인 스냅샷으로 엔진에 병합(스키마 무변경). 프리셋 편집 시 `reqsets:custom` 복제(`source.basePresetId` 보존). 다각도 리뷰 13건 반영 완료. E2E 3경로(프리셋 심화/일반 강제/빈 세트) 검증.

**목표** 처음 쓰는 사람이 자기 요건을 등록할 수 있다.

```
[시작] → 학번 → 학과 → 과정 선택 → 추가 전공 → 예외/학기 → 요건 확인 → 과목 입력 → 백업 안내
   ↑                       ★           ★                        ★
[백업 파일로 복원]
```

**시안 있음** — 온보딩 ①(내 정보), ②(요건 세트 선택)
**시안 없음** — 나머지. 시안 톤을 따라 만들되 **만들기 전에 물어볼 것**

**★ 복원 진입점** — 첫 화면에 `백업 파일 불러오기`를 나란히. 기기를 바꾼 사용자가 처음 만나는 화면이 온보딩이라, 여기 없으면 백업 파일이 있어도 처음부터 다시 입력하게 된다

**★ 과정 선택** — 전체 분기점

| 선택 | 결과 |
|---|---|
| 심화과정 | 전공선택 37, 추가 전공 불필요, 인증 3종 켜짐 |
| 일반과정 | 전공선택 10, **추가 전공 1건 이상 강제**, 인증 3종 꺼짐 |
| 공학인증 | 해당 학과만 |

**★ 추가 전공** — 유형 6종 + 직접 추가. 학점과 중복 인정 상한 **전부 수정 가능**해야 한다

| 유형 | 기본 학점 | 중복 인정 |
|---|---|---|
| 복수전공 | 36 | 6 |
| 부전공 | 21 | 3 |
| 연계전공 | 전공별 | 6 / 3 |
| 융복합트랙 | 15 | 3 |
| 마이크로전공 | 9 | 3 |
| 학생설계전공 · 직접 추가 | 입력 | 입력 |

**★ 요건 확인** — 앱이 채운 숫자를 사용자가 자기 요람과 대조하고 바로 고치는 화면. **실질적으로 가장 중요한 단계.** 부전공 학점처럼 미확인 항목은 눈에 띄게 표시

**데이터 현실** — 지금 학과 선택지에 실데이터가 있는 건 **소프트웨어학과(2021·2022)뿐.** 다른 학과·학번은 프리셋을 추가(`yoram-extract`)하거나 6단계 편집기로 만들 때까지 온보딩에서 고를 수 없다. 위 추가전공 표의 학점·중복 상한은 **편집 가능한 기본값**이며, 근거는 스킬 `references/regulations.md`(연계전공 운영규칙 등) — 사용자가 자기 요람과 대조해 요건 확인 단계에서 고친다.

**완료 기준** — 온보딩만 마치면 요약 화면이 정상 렌더된다 (과목 0건 빈 상태로)

---

### 🟦 착수 전 확인 — 답 기입란 (구현 보류, 답변 대기)

> 각 **Q**의 `답:` 뒤에 A/B/C(또는 자유 기술)를 적어주세요. 비우면 ⭐(추천)안으로 진행합니다.
> **D**(기본값)는 그대로 두면 그대로 진행 — 바꾸려면 `→ 변경:` 뒤에 메모를 남겨주세요.
> "내 정보" 화면은 시안 ①처럼 학번·학과·과정을 한 화면에 묶습니다. 구현은 이 답변을 확인한 뒤 착수합니다.

**Q1 · 단계 구성** — 온보딩을 어떤 단계로 만들까? (과목 입력을 온보딩에 넣을지 포함)
- **A ⭐** 전체 흐름 + 과목 입력은 건너뛰기 — 내 정보 → 과정 → (추가전공) → 예외/학기 → 요건 확인 → 완료. 과목 입력은 5단계 과목 탭에서. 완료 기준(과목 0건 요약) 충족.
- **B** 린 코어 — 내 정보(학번·학과·과정) → 요건 확인 → 완료. 추가전공·예외/학기는 이후 설정·요건 탭에서. (일반과정 추가전공 강제는 요건 확인 단계에서 안내만)
- **C** 명세 그대로 7단계 — 미니 과목 입력 단계까지 온보딩 안에. (5단계 과목 입력 UI와 중복될 수 있음)
- 답: A로 진행.

**Q2 · 프리셋 없는 학과·학번** — 실데이터는 소프트웨어학과 2021·2022뿐. 나머지는 온보딩에서 어떻게?
- **A ⭐** 프리셋 있는 것만 선택 — SW 2021·2022만 선택 가능, 나머지 학번·학과는 "준비 중"으로 비활성. 잘못된 데이터로 오도하지 않음. (yoram-extract로 프리셋 추가 시 자동 개방)
- **B** 빈 세트로 시작 허용 — 아무 학번·학과나 고르고 빈 요건 세트로 시작 → 요건 확인에서 숫자를 직접 채움. (요건 편집기 6단계 전이라 제한적)
- **C** 프리셋 + JSON 붙여넣기 — 프리셋 없으면 선배가 준 요건세트 JSON을 붙여넣어 시작(3단계 공유 불러오기 재사용). 빈 세트 시작은 6단계까지 보류.
- 답: A, B를 혼합. 프리셋 선택에서는 데이터가 있는 학과, 학번만 선택 가능, 나머지는 준비중. 빈세트로 시작하는 경우도 허용, 다만 학과, 학번은 직접 입력 또는 아주대에 존재하는 학과 중 선택 가능으로 사용자에게 자유도를 준다.

**Q3 · '요건 확인' 단계 깊이** — 명세상 가장 중요. 앱이 채운 숫자를 요람과 대조·수정하는 화면.
- **A ⭐** 핵심 숫자 확인+수정 — 총 학점·영역별 최소학점·평점 기준 등 핵심 숫자를 대조·수정하고, 미확인 항목(부전공 verified:false 등)을 강조. **수정 시 프리셋을 복제한 커스텀 세트(reqsets:custom)로 분기.** 영역 추가·필수과목 편집 등 전체 편집은 6단계.
- **B** 읽기 전용 확인 — 숫자를 보여주고 "요람과 대조하세요" 안내만. 수정은 6단계 편집기. (단순하나 '바로 고치는 화면' 취지엔 약함)
- **C** 전체 편집기 앞당김 — 6단계 요건 세트 편집기를 온보딩에 통합. (범위가 크게 늘어남)
- 답: A

**Q4 · '추가 전공' 단계 깊이** — 복수전공/부전공/연계/융복합트랙/마이크로/학생설계 + 직접추가.
- **A ⭐** 유형 선택 + 학점·중복상한 편집 — 기본값은 데이터에서, 학점·중복 인정 상한을 전부 수정 가능(명세대로). 심화=선택, 일반=1건 이상 강제.
- **B** 유형 선택만 — 학점·상한은 기본값 그대로, 세부 수정은 6단계/설정에서.
- **C** 일반과정만 최소 형태 — 심화는 이 단계 건너뜀. 일반과정만 '추가전공 1건 등록'을 최소 형태로.
- 답: A, 다만 일반 과정과 심화 과정이 뭔지 헷갈리는 사람이 있을 수 있으니 선택 박스 밑에 선택한 선택지에 대한 설명이 뜨도록 작업.

**기본값 (그대로 두면 이대로 진행 — 바꾸려면 `→ 변경:` 뒤에 메모)**
- **D1 레이아웃** — 전체화면 마법사(온보딩 중 사이드바·하단독 숨김), 단일 컬럼, 시안 톤, 상단 스텝 인디케이터 + 뒤로가기 + 단계별 검증. → 변경: 
- **D2 복원 진입점** — 첫 화면에 "백업 파일 불러오기"(3단계 개인 백업 복원 재사용) 나란히. → 변경: 
- **D3 예외/학기** — 현재학기·목표졸업학기 입력 + 편입생 여부 + 요건세트가 정의한 예외(majorPolicy.exemptions) 토글. → 변경: 
- **D4 완료(⑦ 백업 안내)** — 완료 화면에서 "백업 받기" 1회 권유(배너/버튼, 모달로 막지 않음). → 변경: 
- **D5 완료 처리** — profile 저장(onboardingCompleted:true) + 요약 렌더. 저장은 adapter 경유. 온보딩 재진입은 설정에서(추후). → 변경: 
- **D6 톤/접근성** — 상태 색 단독 사용 금지, 문구 담백·겁주지 않기 유지. → 변경: 

---

## 5. 요약 · 과목 탭  ✅ 완료

> **상태: 완료.** 요약 탭(도넛·이수/남은/수강중·전체·전공 평점·학기배분 제안·영역별 진행률[부족 우선·충족 접기]·빈 상태·부드러운 판정 배지)과 과목 탭(학기별 목록 PC 테이블/모바일 카드·추가·수정·삭제·삭제 확인·빈 상태) 구현. 순수 로직 `src/app/search.ts`(초성·과목명·학수번호 자동완성)·`src/app/courses.ts`(학기 파싱·상태/성적·`previewBucketId`=엔진 재사용·draft↔Course 변환·검증) + 테스트 40건. 과목 CRUD·`pendingAddCourse`는 `AppState`. 재수강은 `retakeOf` 정식 연결. 다각도 적대적 리뷰 13건 반영(학기 학점 합을 엔진 `countsForCredit`로 재사용·`countsToward` 왕복 보존·학기 정규화 등). 전체 227 테스트·빌드·E2E(초성 검색·요약/과목/폼 렌더) 통과.

**목표** 실사용 가능. 여기까지가 최소 단위.

**요약 탭** — 시안 있음

- 전체 진행률, 총 이수학점, 전체/전공 평점
- **부족한 것만 위로.** 충족 항목은 접는다
- 문구는 짧게: "전필 1과목 부족", "영어 인증 미완료"
- 다음 단계 제안 (남은 학점 ÷ 남은 학기)
- 참고용 고지 상시 노출

**과목 탭** — PC 테이블 시안 있음, 모바일 카드 리스트와 입력 폼은 없음

- 학기별 목록, 과목 추가·수정·삭제
- 과목 상태: 예정 / 수강중 / 완료 / 재수강 / 편입인정 등
- 검색 자동완성 — 과목명·학수번호·**초성** 지원
- 빈 상태 시안 있음 ("아직 과목이 없어요")

**완료 기준** — 과목을 넣으면 요약 진행률이 즉시 반영된다

---

## 6. 요건 탭 + 편집기  ✅ 완료

> **상태: 완료.** 요건 탭(`src/app/tabs/RequirementsTab.tsx`) = 헤더(세트 이름·전환/복제/편집) + 학점 요건 읽기(영역별 진행·미이수 사유) + 비교과 체크리스트(유형별 정확 입력) + "요건은 데이터입니다" 안내. 순수 편집 로직 `src/app/reqset-editor.ts`(복제·startEdit·영역/필수과목/택1·비교과 정의 CRUD·유형전환 정리·검증·비교과 상태 입력) + 테스트 37건. 하위 컴포넌트 `tabs/requirements/`(NonCurricularChecklist·CoursePicker·ReqSetEditorModal). AppState 액션 5종(setNonCurricular·saveEditedReqSet·cloneActiveReqSet·switchReqSet·deleteCustomSet). 공유 내보내기/불러오기는 3단계 설정 탭 재사용(불러오기 id 충돌 시 새 id 부여로 항상 추가). 전체 264 테스트·빌드·E2E(요건탭·비교과 입력 판정 반영·편집 저장·검증 차단·세트전환·삭제확인·level scale 검증) 통과. 5차원 적대적 리뷰 17건 반영(핵심: scale 없는 level 무조건 충족 → 엔진 가드+검증+scale UI, 공유 id 덮어쓰기 방지). **결정(2026-07-22)**: 편집 UX=전용 모달, 범위=영역+비교과 정의, 비교과 입력=유형별 정확, 복제·전환=요건 탭 헤더.

**목표** 2021 SW 말고 다른 학과·학번도 쓸 수 있다.

- 영역별 진행 + 비교과 체크리스트 (시안 있음)
- **요건 세트 편집기** (시안 없음) — 영역 추가·삭제, 최소 학점 수정, 필수 과목 편집, 택1 그룹 편집
- 세트 복제 ("2021 SW" → "2025 SW")
- 세트 전환
- JSON 공유 내보내기/불러오기

**새 세트 만들 때 주의** — 세트 복제로 다른 학번을 만들 땐 숫자를 그대로 베끼지 말 것. 졸업 총학점은 입학년도마다 다르고(2021·2022 SW는 140, 현행 학칙은 소융대 128), 대학·학과명과 소속도 바뀐다(예: 소프트웨어학과는 2022까지 정보통신대학 소속). 통합·개편된 학부(프런티어과학학부·경제정치사회융합학부 등)는 내부 전공별로 요건이 갈릴 수 있다. 근거·이력은 스킬 `references/regulations.md`·`references/naming.md`.

**완료 기준** — 앱 안에서만 조작해 다른 학번용 세트를 만들 수 있다

---

## 7. 백업 리마인더 + 설정  ✅ 완료

> **상태: 완료.** 백업 리마인더 판정은 순수 모듈 `src/app/backup-reminder.ts`(+테스트 20건) — `evaluateBackupReminder`(no_backup/stale/many_new + 학기말 강조)·`semesterEndPeriod`(6·12월 하순)·`backupStatus`(설정 표시용). 요약 탭 상단 배너(`Banner` 확장: info/emphasis 톤·닫기X·포커스링), 설정 탭 강화(백업 상태 요약·저장소 상태·온보딩 다시 하기·초기화 확인 문구 입력). `Settings`에 `bannerDismissedAt`·`courseCountAtLastBackup`·`semesterNudgeShownFor` 추가(SCHEMA_VERSION 유지, defaultSettings 병합으로 구버전 백업 호환). 사용자 확정(2026-07-22): 학기말=학기당 1회 강조(7일 닫기 무관), 초기화=확인 문구('초기화') 입력. 4렌즈 적대적 리뷰 7건 확정 반영 — 핵심: 온보딩 재진입 시 완료화면 백업이 라이브 과목을 포함(반쪽 백업+false 기준선 방지), export가 갱신 후 settings를 파일에 기록, 닫기는 표시 중 배너 기준(학기말 오소진 방지), 미래 dismissedAt 억제 안 함, info 배너 대비 상향. 전체 284 테스트·빌드·Playwright(배너·설정·초기화 문구입력) 통과.

**백업 배너 조건**

| 조건 | 동작 |
|---|---|
| 백업 이력 없음 + 과목 5건 이상 | 요약 탭 상단 배너 |
| 마지막 백업 후 30일 경과 | 배너 |
| 마지막 백업 이후 과목 10건 이상 추가 | 배너 |
| 배너 닫음 | 7일간 숨김 |
| 학기말 (6월 말·12월 말) | 1회 강조 |

**모달로 막지 말 것.** 배너 1줄 + 버튼. 문구는 겁주지 않게.

**설정 탭**

- 마지막 백업 시각, 저장된 데이터 요약
- 백업 받기 / 불러오기 / 요건 세트 공유
- 전체 초기화 (2단계 확인, 초기화 전 백업 권유)
- 상시 안내: "이 앱은 서버에 아무것도 저장하지 않습니다"

---

# 개편 로드맵 — 데이터 생산 라인 (R단계)

핵심 로드맵(0~7)과 별개로, **데이터 확장의 비용 구조를 바꾸는 작업.** 진단·측정치·제안 원문은 `개선_방향_제안.md`, 결정 근거는 그 문서 §12. 아래 결정 5건은 위임에 따라 확정된 값이다(2026-07-24).

**운영 규칙**

- **R3 완료 전 신규 학과·학번 데이터화 착수 금지** — 모든 신규 세트가 백로그 10의 함정을 복제 중이므로, 전역 결정을 먼저 끝낸다
- **R5 완료 후 신규 건은 새 라인만 사용** — 완전본 JSON 손 작성·학번별 테스트 파일 신설 금지
- 각 R단계는 독립 커밋·독립 롤백 가능해야 한다. 데이터 산출물이 바뀌는 커밋은 스냅샷(R0) 갱신을 명시적으로 포함한다

## 결정 기록 (2026-07-24 — 위임 확정)

| # | 항목 | 결정 |
|:--:|---|---|
| ① | T1(전 검증) 커트라인 | **8개 학과**: 소융대 5(sw·ai·sec·media·ddc) + 기계(me) + 전자(ee) + 경영(mgmt), 전 학번. 신규 T1 약 40건. 나머지 요람 보유 학과는 T2(초안), 요람 없는 곳·신설 단위는 T3. 수요 신호(이슈)로 T2→T1 승격 |
| ② | T2 초안 노출 | **노출한다.** `verified:false` 세트에 "초안 — 요람 대조 전" 배지 + 온보딩 요건 확인 단계에서 강조. 요건 확인 단계가 원래 "요람과 대조하는 화면"이므로 취지 일치 |
| ③ | 백로그 4·10 정책 | **10**: 엔진 암묵 이월 채택 — free 버킷 충족 계산에 타 버킷 초과분 합산(데이터 무변경, 전 세트 적용). 세트마다 creditCap을 뿌리는 안은 기각. **4**: 요람 문구 충실 — general 세트 `industry_project`를 `appliesWhen null`+`pick 1`로 활성화. major_ability·programming_cert는 요람 재확인 후 판단(기본: 심화 전용 유지) |
| ④ | 상속 모델 적용 범위 | **역이식 포함** — 기존 SW 6학번 + ME 2학번 + GE 카탈로그 6종 전부 소스 포맷으로. 재생성=커밋본 동일 게이트로 위험 통제 |
| ⑤ | 커뮤니티 세트 수용 | **보류.** 수요 신호(이슈 템플릿)만 먼저. 티어 체계가 안정된 뒤 재검토 |

---

## R0. 산출물 스냅샷 고정  ✅ 완료

> **상태: 완료(2026-07-24).** `data-snapshot.test.ts` — 전 JSON 30건 정규화 해시 고정, import.meta.glob 열거로 파일 추가·삭제 양방향 드리프트 감지. 갱신은 `node scripts/data-snapshot.mjs`.

**목표** 개편 기간 동안 데이터 산출물의 의도치 않은 변화를 기계적으로 차단.

**만들 것** — `src/engine/__tests__/data-snapshot.test.ts`: 등록된 전 번들(세트·카탈로그·추가전공 규칙·GE 6종)의 정규화 해시(키 정렬 stringify → sha256)를 고정 목록과 대조.

**완료 기준** — 스냅샷 테스트 통과. 이후 데이터가 바뀌는 커밋은 해시 갱신을 함께 포함해야만 통과한다(드리프트 조기 발견).

---

## R1. 등록 자동화 — 매니페스트  ✅ 완료

> **상태: 완료(2026-07-24).** `src/data/manifest.ts` — glob+파일명 파싱으로 registry·geCatalogByYear·DATA_BUNDLES·bundlesByDepartment 자동 구성, 구성 결손은 로드 시점 즉시 실패. index.ts named export는 매니페스트 조회로 유지, dataSources 손 매핑(ME_BUNDLES·BUNDLE_20xx)·merge.ts GE_CATALOG_YEARS 손 상수 제거(동작 불변). 정합 테스트 29건(`manifest.test.ts`).

**목표** "3곳 수동 등록 누락 → 조용한 오동작" 오류 계급 소멸.

**만들 것**

```
src/data/manifest.ts   import.meta.glob('./…-*.json', { eager: true }) + 파일명 파싱
                       → requirementSetRegistry · geCatalogByYear · DATA_BUNDLES 자동 구성
```

- `index.ts`의 손 import·손 레지스트리, `dataSources.ts`의 손 매핑 제거. **기존 named export는 매니페스트 조회로 재구성해 유지**(테스트·앱 호환)
- 매니페스트 정합 테스트: 세트 `department` ↔ `ajou-departments.ts` 이름 일치, 세트 id ↔ 파일명 학번·slug 일치, 번들 구성 누락 0
- glob은 `eager: true` 유지 — 번들 동작 불변(지연 로딩은 R7)

**완료 기준** — 기존 전 테스트 + 스냅샷 통과, 손 등록 코드 소멸. 새 JSON 파일을 넣기만 하면 앱·테스트에 등록된다.

---

## R2. 범용 무결성 스위트  ✅ 완료

> **상태: 완료(2026-07-24).** `data-invariants.test.ts` — 전 번들 순회 346건(카탈로그 A-01~08 / 세트 B-01~14 / 추가전공 C-01~02), 의도된 예외는 허용 목록 데이터(중복 학수번호·미채택 ajou_hope·제외 영역·dangling from). 학번별 파일에서 중복 절 -532줄(커버 대조표: `데이터_테스트_커버_대조표.md` — 계획·실행 결과·미이관 항목 기록). 부수 발견: GE 학수번호 교차 부여 7건 → 백로그 13번.

**목표** 무결성 검사를 데이터 건수와 무관한 상수 비용으로.

**만들 것** — `src/engine/__tests__/data-invariants.test.ts`: 매니페스트의 전 번들을 순회하며 A-계열 검사 전부 적용(courseKey 유일성 / requiredCourses·choiceGroups·courseGroups·비교과 그룹 키 존재 / linkedTo / 영역 정합 / `group:'free'` 폴백 1개 / defaultBucket·capBucket·overflowTo 유효 / AM 타입 / 학수번호 중복 의도 목록). 의도된 예외(INTENDED_DANGLING 등)는 번들별 허용 목록 **데이터**로 관리.

**그 후** — 학번별 `data-*.test.ts`에서 범용 스위트가 커버하는 절을 점진 삭제(삭제 전 커버 대조표 작성), 학과 고유 사실만 남긴다.

**완료 기준** — 신규 번들 추가 시 무결성 테스트 코드 0줄. 삭제 후에도 검사 커버리지 동일함을 대조표로 확인.

---

## R3. 전역 결정 반영 (데이터화 재개 게이트)

**목표** 전 세트 공통 결정을 세트가 적을 때 끝낸다. 4건 각각 독립 커밋.

| | 항목 | 내용 |
|:--:|---|---|
| R3a | 백로그 10 | 엔진 암묵 이월 — `aggregate`에서 free 그룹 버킷 충족 계산에 타 버킷 `minCredits` 초과분 합산(명시 `overflowTo`로 이동한 분 제외, 이중 계산 가드). UI에 "전공선택 초과 N학점이 잔여 학점으로 집계됐어요" 사유 표시. 기계 인증 41학점 케이스 재현 테스트 |
| R3b | 백로그 4 | 2021~2024 SW `*-general` 4개 세트 `industry_project` → `appliesWhen null`+`pick 1`. B-05 계열 테스트 갱신. major_ability·programming_cert는 요람 원문 재확인 후 별도 판단 |
| R3c | 백로그 11 | 하드코딩 안내 문구 2건 중립화, 사유는 데이터(`courseGroups[].label`/신설 `note`)에서 읽기 |
| R3d | 백로그 5 | `MajorPolicy`에 유형별 최소 개수(`countsByType: { micro_degree: 2 }`) — 2025·2026 세트 `am_micro` 갱신 |

**완료 기준** — 각 항목 테스트 포함, 스냅샷 갱신 명시, `데이터_정정_백로그.md` 상태 갱신(⬜→✅).

---

## R4. 상속(패치) 모델 — 소스·산출물 분리

**목표** 사람이 쓰는 것은 "바뀐 것"뿐. 런타임 포맷·엔진·앱은 무변경.

**만들 것**

```
data-src/
  ge/buckets-{학번}.json        교양 공통 버킷 조각 (전 학과 공유)
  sw/base.json                  학과 베이스 (첫 학번 완전본)
  sw/{학번}.patch.json          그 해 바뀐 것만 ($remove·$include 지원)
  sw/tracks.patch.json          과정 간 차이 (심화 vs 일반)
scripts/lib/merge.ts            id/courseKey 키 병합 — 순수 함수 + vitest 단위 테스트
scripts/build-data.ts           node 직접 실행(Node 26 type stripping, 신규 의존성 0)
```

- npm 스크립트: `data:build`(생성) · `data:check`(재생성 결과 = 커밋본 일치 게이트)
- 직렬화 규칙을 빌더에 고정하고, 커밋본을 그 규칙으로 **1회 재포맷**한 뒤부터는 바이트 동일 게이트
- 역이식 순서: **SW 6학번 → 게이트 통과 → ME 2학번 → GE 카탈로그 6종**
- `CLAUDE.md` '하지 말 것'에 "산출물 JSON 직접 수정 금지 — `data-src/` 수정 후 `data:build`" 추가

**완료 기준** — `data:check` 통과, 기존 전 테스트 통과, 런타임 코드 무변경. 2024 SW의 패치 파일이 실제로 "전선 2과목 제외 + 문구 1건" 수준으로 짧은지 확인(모델 검증).

---

## R5. 요람 기대값 명세 + 판정 시나리오 생성

**목표** 건당 검증 산출물을 "TS 수백 줄"에서 "기대값 JSON 수십 줄"로.

**만들 것**

- `data-src/expected/{학번}-{slug}.json` — 요람 인쇄면 소계: 총학점·버킷별 minCredits·전필 수·카탈로그 수·creditBreakdown 소계·영역 구성. 범용 비교기 테스트 1개가 전 번들 대조
- 합성 성적표 생성기 — 세트에서 만점 성적표 생성 → `graduatable`, 전필 1개 제거 → `not_graduatable`, 수강중 전환 → `graduatable_after_current`. 전 번들 자동 적용, 수제 성적표는 SW·ME 대표만 유지
- `yoram-extract` 스킬 문서(§3·§5·§6)를 새 라인 기준으로 갱신 — 산출물 = 패치 + 기대값 블록

**완료 기준** — 기존 SW·ME 번들로 역검증(현행 테스트 기대값과 일치). 이후 신규 건에서 학번별 테스트 파일을 만들지 않는다.

---

## R6. 티어제 커버리지

**목표** "없는 학과"의 사용자도 온보딩만으로 시작할 수 있다.

- **T3 뼈대 생성기** — 온보딩 '빈 세트' 경로 확장: (학번·계열) 입력 → 교양 공통 버킷(R4 조각 재사용) + 학칙 전역값을 채운 뼈대 세트 생성 → 요건 확인 단계에서 전공 숫자만 입력. `reqsets:custom` 저장
- **T2 초안 노출** — `verified:false` 세트에 "초안 — 요람 대조 전" 배지(온보딩·요건 탭). 문구는 겁주지 않는 톤
- **수요 신호** — `.github/ISSUE_TEMPLATE/preset-request.yml` + 앱 내 "우리 학과가 없어요" 링크(온보딩 학과 선택·설정 탭)

**완료 기준** — 프리셋 없는 학과 사용자가 온보딩만으로 자기 세트를 만들어 요약이 렌더된다(E2E 1경로).

---

## R7. (조건부) 지연 로딩

데이터 총량이 초기 번들 1MB(gzip 전)를 넘보면 착수. R1 glob을 `eager: false`로 전환 + `dataSources` 비동기화. 그 전에는 하지 않는다.

---

## 데이터화 재개 기준

- **R3 완료 후** T1 학과 재개 가능(권장 순서: ai → sec → media → ddc → me 2023~2026 → ee → mgmt)
- **R5 완료 후** 신규 건은 새 라인(패치 + 기대값)만 사용 — R4·R5 전에 재개한 건이 있으면 역이식 목록에 추가
- T2(초안)는 T1 소진 후 또는 수요 신호 발생 시. 통합학부(경제정치사회융합·프런티어과학·바이오)는 내부 전공 분화 확인 전 착수 금지(기존 주의사항 유지)

---

## 진행 중 확인할 것

애매하면 **추측하지 말고 물어볼 것.** 특히:

- 시안에 없는 화면을 새로 만들 때
- 요건 해석이 갈릴 때 (요람 표가 모순되는 경우가 실제로 있다)
- 아키텍처 4대 규칙을 어겨야 할 것 같을 때 — 대개 다른 방법이 있다
