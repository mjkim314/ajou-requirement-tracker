# 아주 졸업요건 트래커

아주대 학생이 자기 학번·학과·과정에 맞는 졸업요건을 등록해두고, 들은 과목을 입력하면 뭐가 얼마나 남았는지 바로 보는 웹앱.

**서버 없음. 계정 없음. 브라우저 로컬 저장 + 파일 백업.**

---

## 참고 파일 (별도 제공)

| 파일 | 용도 | 다룰 때 |
|---|---|---|
| `아주_졸업요건_트래커_시안_dc.html` | **확정된 디자인 시안.** 색·타이포·간격·컴포넌트 형태의 기준 | UI를 만들 때 **반드시 먼저 열어볼 것** |
| `support.js` | 시안 HTML의 렌더 런타임 | **읽기만.** 제품 코드에 포함하지 않음 |
| `아주_졸업요건_트래커_설계서_v1.0.md` | 작업 기준 설계서 | 기능·플로우 판단이 필요할 때 |
| `아주_졸업요건_트래커_설계명세서_v0.2.md` | 상세 명세 (JSON 스키마, 계산 규칙, 테스트 45건) | 구현 세부가 애매할 때 |
| `2021요람_소프트웨어학과.pdf` | 원본 요람 | 요건 숫자를 확인할 때 |

### 시안을 다루는 규칙

- 시안은 **정적 목업**이다. 인라인 스타일로 되어 있고 `<x-dc>` 커스텀 태그와 `support.js` 런타임 위에서 돌아간다. **이 구조를 제품으로 옮기지 말 것.**
- 가져올 것: **색상 값, 폰트, 여백 리듬, 라운드 값, 컴포넌트 생김새, 정보 배치**
- 버릴 것: 인라인 스타일 방식, `<x-dc>` / `<helmet>` 태그, `support.js` 의존, 2400px 캔버스 레이아웃
- 시안에 없는 화면은 **시안의 톤을 따라 새로 만들되, 만들기 전에 물어볼 것**

### 시안이 덮는 범위

| 있음 | 없음 (새로 설계 필요) |
|---|---|
| 온보딩 ① 내 정보 | 온보딩 ③~⑦ (추가 전공, 예외, 요건 확인, 과목 입력) |
| 온보딩 ② 요건 세트 선택 | 요건 세트 편집기 |
| 요약 탭 (진행률·평점·다음 단계) | 백업·복원 화면 |
| 요약 탭 빈 상태 | 과목 추가/편집 폼 |
| 과목 테이블 (PC) | 모바일 과목 카드 리스트 상세 |
| 요건 탭 (학점 요건 + 비교과 체크리스트) | 설정 탭 전체 |

---

## 디자인 토큰

시안에서 추출한 값. 이 값을 Tailwind 설정이나 CSS 변수로 한 번만 정의하고, 컴포넌트에서 하드코딩하지 말 것.

```
--navy      #0B2E5F   주색 · 헤더 · 강조 텍스트
--blue      #2E6FE0   액티브 · 진행 중 · 링크
--green     #1F9D6B   완료 · 충족
--orange    #D9822B   주의 · 부족
--ink       #16202E   기본 텍스트
--ink-2     #3A4658   보조 텍스트
--ink-3     #5B6577   설명 텍스트
--muted     #8792A2   비활성 라벨
--muted-2   #9AA5B3   더 옅은 라벨
--line      #E4E9F0   경계선
--line-2    #EEF1F6   옅은 경계선
--surface   #FFFFFF   카드
--bg-soft   #F5F7FA   카드 안 블록
--bg        #E7EBF1   페이지 배경
```

- **폰트**: Pretendard (`cdn.jsdelivr.net/gh/orioncactus/pretendard`)
- **아이콘**: Material Symbols Rounded
- **라운드**: 카드 20px / 블록 13px / 작은 요소 9px
- **그림자**: `0 1px 2px rgba(11,46,95,.04), 0 10px 30px rgba(11,46,95,.05)`
- **상태 색은 단독으로 쓰지 않는다.** 색맹 대응으로 아이콘이나 텍스트를 항상 함께 붙일 것

---

## 스택

| 항목 | 선택 |
|---|---|
| 프레임워크 | React + Vite |
| 언어 | TypeScript |
| 스타일 | Tailwind |
| 저장 | **localStorage** |
| 백엔드 | 없음 |
| 배포 | 정적 호스팅 |

> **설계서 v0.2의 "localStorage 미사용"은 무시할 것.** 그건 초안을 쓰던 환경의 제약이었다. 실제 정적 웹앱에서는 localStorage가 정답이다. 데이터가 수백 KB 수준이라 IndexedDB까지 갈 필요 없다.

---

## 아키텍처 — 이 4가지는 지킬 것

```
화면 (React)
   ↓
계산 엔진 (순수 함수)
   ↓
Storage Adapter
   ↓
localStorage  ← 나중에 서버로 바꿀 수 있게
```

| # | 규칙 | 이유 |
|---|---|---|
| **1** | **계산 엔진은 순수 함수.** 입력(프로필·과목·요건·카탈로그) → 출력(판정 결과). React·localStorage·`Date.now()`를 직접 참조하지 않는다 | 화면 없이 테스트 가능해야 함 |
| **2** | **localStorage 접근은 Storage Adapter 안에서만.** 컴포넌트에서 직접 부르지 않는다 | 나중에 계정을 붙이면 이 계층만 교체 |
| **3** | **요건 숫자를 코드에 하드코딩 금지.** 140, 36, 2.0 전부 요건 세트 JSON에서 읽는다 | 학번·학과마다 다르고 매년 바뀜 |
| **4** | **과목은 이름이 아니라 `courseKey`로 참조.** 학수번호는 배열 속성일 뿐 | 요람 안에서도 SCE411이 두 과목에 중복 부여돼 있음 |

3번과 4번은 이 프로젝트의 존재 이유에 가깝다. 어기면 앱이 2021년 소프트웨어학과 전용 계산기가 된다.

---

## 데이터

### 저장 키

각 키는 덩어리로 저장하고 `updatedAt`을 포함한다.

| 키 | 내용 |
|---|---|
| `profile` | 학번, 학과, 과정, 추가전공, 예외, 학기 |
| `courses` | 수강 과목 전체 배열 |
| `noncurricular` | 비교과 진행 상태 |
| `reqsets:custom` | 사용자가 만들거나 수정한 요건 세트 |
| `settings` | 표시 옵션, 마지막 백업 시각 |

`updatedAt`은 지금 당장은 안 쓰지만 **반드시 넣을 것.** 나중에 동기화를 붙일 때 필요하고, 지금 넣으면 공짜다.

### 번들 데이터 (localStorage 아님, 코드에 포함)

- `data/requirement-set-2021-sw.json` — 2021 SW 기본 프리셋
- `data/catalog-2021-sw.json` — 교과목 카탈로그

---

## 하지 말 것

| 금지 | 대신 |
|---|---|
| 요건 숫자 하드코딩 | 요건 세트 JSON에서 읽기 |
| **산출물 JSON(`src/data/*.json`) 직접 수정** | `data-src/` 수정 후 `npm run data:build` (R4 상속 모델 — `data-src/README.md`) |
| 과목을 이름 문자열로 비교 | `courseKey`로 비교 |
| 컴포넌트에서 localStorage 직접 호출 | Storage Adapter 경유 |
| 계산 로직을 컴포넌트 안에 작성 | 엔진 모듈로 분리 |
| 시안의 인라인 스타일 복사 | 토큰 + Tailwind 클래스 |
| `support.js`를 제품에 포함 | 시안 볼 때만 사용 |
| 백업 기능을 나중으로 미루기 | 3단계에서 구현 (아래 참조) |
| "졸업 가능"을 단정적으로 표기 | 참고용 고지 항상 병기 |

---

## 작업 순서

`TASKS.md` 참조. 요약하면:

1. 계산 엔진 (순수 함수)
2. 프리셋 + 카탈로그 데이터
3. **저장 계층 + 백업·복원**
4. 온보딩
5. 요약 · 과목 탭
6. 요건 탭 + 편집기
7. 백업 리마인더 + 설정

**3번을 앞에 두는 이유** — 화면부터 만들면 개발 중 테스트 데이터가 계속 날아가고, 나중에 저장 구조를 바꾸면 이미 쌓인 데이터를 옮겨야 한다.

---

## 미해결 사항

| 항목 | 처리 |
|---|---|
| **부전공 전공학점** | 요람 표 병합 셀상 36으로 읽히나 헤더는 46 표기. **36으로 넣되 프리셋에 `"verified": false` 표시.** 온보딩 요건 확인 단계에서 눈에 띄게 노출 |
| 학번별 영역별교양 | 2021만 프리셋 제공. 나머지는 사용자 편집으로 대응 |
| 평점 테이블 | 4.5 만점 기본값. 요건 세트에서 편집 가능하게 |

---

## 톤

- 한국어 UI. 문구는 짧고 담백하게
- 겁주지 않는다. "데이터가 사라집니다" 대신 "백업한 지 30일 됐어요"
- 졸업 판정은 **참고용**이라는 고지를 요약 화면에 상시 노출
- 부족한 항목을 나열할 때 죄책감을 주지 않는다. 사실만 짧게
